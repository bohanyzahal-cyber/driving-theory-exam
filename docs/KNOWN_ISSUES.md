# מלכודות ידועות ותקלות שכבר קרו

כל פריט: **תסמין → שורש הבעיה → מצב**. רובן עלו מתקלות אמת ביום בחינות. קרא את הרשימה לפני שאתה מאבחן מחדש משהו שכבר נפתר.

**מקרא אמינות:**
`✅` אומת ישירות בקוד/במערכת החיה · `📋` מהיסטוריית הפרויקט — אמת לפני שאתה מסתמך

---

## א. פריסה ומטמון

### 1. `✅` "פרסתי ואני עדיין רואה את הגרסה הישנה"

**שורש:** שלוש שכבות מטמון מצטברות — service worker לכל דף, מטמון HTTP של GitHub Pages (600 שניות), והעובדה שהכתובות `/examiner/` וכו' הן **קליפות PWA שטוענות את ה-HTML ב-iframe**. בדיקת ה-DOM של המסמך העליון מראה את הקליפה, לא את האפליקציה.

**אבחון:** שליפה טרייה עם עוקף מטמון וחיפוש סמן ייחודי — ראה `OPERATIONS.md` §1. אל תסיק מהצצה בדפדפן.

**כפיית עדכון לכל הצי:** שינוי `CACHE_NAME` ב-service worker הרלוונטי.

### 2. `✅` דחיפת דף נבחן מקפיצה נבחנים באמצע

**תסמין:** נבחן שממתין לאישור נזרק לטופס ההרשמה ומשם חזרה להמתנה, עם "שגיאת תקשורת".

**שורש:** כל דף בודק את ה-ETag שלו כל 2 דקות ומרענן את עצמו כשזוהתה גרסה חדשה. המגן כיסה רק מבחן פעיל, לא נבחן שנרשם וממתין.

**מצב:** תוקן 28/07/2026 — אין רענון כשיש נבחן רשום. **`student.html` עדיין חשוף לדפוס דומה.**

### 3. `📋` שינוי בקובץ ה-Apps Script לא משפיע

**שורש:** `git push` לא פורס את השרת, ו"שמירה" בעורך לא מספיקה — חייבים `New version` בפריסה.

**מצב:** נוהל קבוע. ראה `OPERATIONS.md` §2.

### 4. `✅` עריכת JSON של שאלות לא עושה כלום

**שורש (היסטורי, עד 22/09/2026):** בנק השאלות החי היה תיקיית Drive פרטית. **מאז 22/09/2026** הבנק סטטי ב-Pages (`bank/`) ומיוצר מ-`deployment/generated/` — עריכה = `node tools/build.js` + `git push` (`OPERATIONS.md` §4).

**מצב:** נוהל ב-`OPERATIONS.md` §4. **נכון ל-22/07/2026 דווח שהעברית, רוסית, ערבית, ספרדית ואמהרית לא סונכרנו** — אנגלית וצרפתית כן. לאמת מול המערכת החיה.

---

## ב. ביצועים וזמינות

### 5. `📋` (היסטורי — המטמון הוסר 22/09/2026) "אין תקשורת" יומי סביב 13:00, כרבע שעה, נפתר מעצמו

**שורש:** מטמון השאלות פוקע אחרי 6 שעות → גל תחילת המבחנים של אחר הצהריים פוגע במטמון קר → מבול קריאות Drive מרווה את חוטי הביצוע של Apps Script. נבחנים שכבר בתוך מבחן לא מושפעים (המבחן מקומי).

**לא** עומס — בבקרים יש יותר עומס והם תקינים.

**מצב:** תוקן (נעילת stampede + טריגר חימום). אם חוזר — בדוק שהחימום רץ.

### 6. `📋` היתקעויות בוקר (9:00-11:00)

**שורש:** דשבורד הבוחן (פולינג כל 5 שניות לכל בוחן) סרק את גיליון התוצאות ההולך וגדל בתוך לולאה לכל נבחן. גדילת **נתונים** היא שהפילה אותו, לא קוד חדש.

**מצב:** תוקן באינדוקס. נפרס 28/07/2026.

### 7. `📋` (היסטורי — המטמון הוסר 22/09/2026) מטמון שאלות מורעל

**תסמין:** "לא נמצאו שאלות", עברית נשברת ראשונה.

**שורש:** תנאי מרוץ בין חימום לקריאה חיה יכול לאחסן `[]`.

**מצב:** יש שומרים הגנתיים. שחזור: `emergencyClearAndRefreshCache()`.

---

### 7א. `📋` "אין תקשורת" בכל המערכת בבת אחת — בכל המכשירים, כולל תרגול

**תסמין (אירוע 19/08/2026):** יום שלם של "אין תקשורת" לסירוגין — אצל בוחן, נבחן **וגם בדף התרגול**, גם בטלפונים (סלולר) וגם במחשבי כיתה. בודדים הצליחו להתחבר, ולאט. באותו יום דווחה מתקפת סייבר על אתרים ממשלתיים בישראל.

**האבחנה:** כשהכשל חוצה את *כל* האפליקציות ואת *כל* סוגי הרשת בו-זמנית, הגורם המשותף היחיד הוא **הדרך מישראל לשרתי גוגל בחו"ל** — לא המערכת. בזמן מתקפת DDoS ארצית, ספקיות והגנות הסינון מדרדרות תעבורה בינלאומית לסירוגין; המערכת אינה יעד, אבל הכביש אליה פקוק. יש תקדים מתועד: מתקפה על ספקית תקשורת שחסמה גישה לאתרים רבים.

**איך מכריעים (בזמן אירוע):**
1. probe מתוזמן מול ה-API (ראה `OPERATIONS.md` §2) מרשת אחרת/VPN — אם עונה מהר משם, הבעיה בדרך, לא בשרת.
2. **הבדיקה המכריעה:** יומן הביצועים של Apps Script (עורך הסקריפט → ביצועים/Executions) בחלון התקלה — כמעט ריק = הבקשות לא הגיעו כלל (רשת); מלא בשגיאות/ריצות ארוכות = בעיית שרת (סיפור אחר, ראה §5-6).
3. אתרים זרים כלליים איטיים/קטועים מאותה רשת = אישוש נוסף לבעיית דרך.

**מה עושים:** אין מה לתקן במערכת — זה חולף כשהאירוע נגמר. נבחנים שכבר בתוך מבחן ממשיכים כרגיל (המבחן מקומי); הצוואר הוא התחלה והגשה. תוצאה שנתקעה בהגשה נשלחת שוב אוטומטית בטעינת הדף — לוודא למחרת מול הגיליון. **לא לפרוס שינויים באמצע אירוע כזה** — אי אפשר להבחין בין תקלת פריסה לתקלת רשת.

## ג. נתונים ונכונות

### 8. `📋` `ci` שגוי בהחלפת שפה

**שורש:** לקובצי ה-JSON של התרגומים **אין** שדה `ci`. חייבים לחשב דרך `lookupCorrectIndex(id, lang)` ולא לשלוף מה-JSON.

**מצב:** מלכודת חוזרת. בכל לולאה על תרגומים — ודא את זה.

### 9. `📋` מבחן שהוגש כ-0/30 עם "תשובה נכונה: undefined"

**שורש:** השרת מסר שאלות בלי `ci`, הלקוח פענח זבל והאינדקס הנכון יצא -1.

**מצב:** ציון השרת נשאר אמין; רק החישוב בצד הלקוח נשבר. **חסר עדיין שומר בצד השרת** — ראה `TODO.md`.

### 10. `📋` תוצאות כפולות אחרי פסילה

**שורש:** ביטול פסילה שטיפל רק ב-`disqualified` והשאיר `dq_confirmed`.

**מצב:** תוקן. אל תחזיר אחורה בלי לטפל בשני המצבים.

### 11. `📋` "מורה: לא ידוע" בדו"חות

**שורש:** מחיקת כיתה מוחקת את שורת הכיתה ומשאירה את התוצאות יתומות.

**מצב:** נוסף ארכיון `כיתות שנמחקו`. **רק מחיקות עתידיות מתועדות** — יתומים היסטוריים יישארו.

### 12. `✅` `SHEET_HEADERS` לא מתאר את המצב בפועל

**שורש:** הקבוע הוא תבנית ליצירת גיליון. `ממתינים` מתועד עם 15 עמודות, הקוד כותב עד 19.

**מצב:** מתועד ב-`DATA_MODEL.md`. עדכן את שניהם בכל עמודה חדשה.

### 13. `✅` שתי כתובות API שונות

**שורש:** `report.html` מצביע על deployment אחר של Apps Script משאר ששת הדפים.

**מצב (21/09 ערב):** נחקר — שריד. `report.html` נוצר 24/02/2026 ונשכח כשכתובת השרת הוחלפה ב-04/03 (e89a729); שום דף לא מקשר אליו מאז יולי; הפריסה הישנה עונה בלי `build` (קוד ישן, בלי תיקוני האבטחה מאז מרץ, מחובר לגיליונות החיים) ומדליפה פרטים פנימיים בהודעת שגיאה. **ממתין להחלטת יוסי** (TODO 0.5): למחוק את הדף ולארכב את הפריסה הישנה.

---

## ד. מכשירים ודפדפנים

### 13א. `✅` פסילה מיידית בכל מבחן — רק בחלק ממחשבי הכיתה

**תסמין (תקלה חיה 02/08/2026):** בחלק קטן מהמחשבים, כל מבחן נפסל שניות אחרי תחילתו. ביטול הפסילה לא עזר — החזרה למבחן פסלה שוב מיד. מעבר למחשב אחר פתר.

**שורש:** זום דפדפן שהוגדר במחשב מראש (Ctrl+גלגלת קורה בטעות בכיתות; הדפדפן זוכר זום לכל אתר). בדיקת המסך-המפוצל השוותה רוחב חלון ב-CSS px (מתכווץ תחת זום) מול בסיס מסך פיזי (לא מושפע) — זום ≥ ~112% הפיל את היחס מתחת לסף ה-90%, והבדיקה שרצה 800ms אחרי ההתחלה פסלה. זיהוי הזום הקיים לא כיסה זאת כי ה-baseline שלו נלכד בתחילת המבחן, כשהזום כבר בפנים.

**עקיפה חיה:** `Ctrl+0` בדפדפן של המחשב הבעייתי (מאפס זום ל-100%).

**מצב:** תוקן (`0a839c8`) — המדידה בשולחני עברה ל-outerWidth/outerHeight, יחידות חסינות-זום. חלון מפוצל באמת עדיין נתפס.

### 14. `📋` iPad נפסל מיד

**שורש:** iPadOS 13+ מדווח על עצמו כ-Macintosh, מה ששבר את זיהוי המכשיר. iPad לרוחב נפסל אוטומטית.

**מצב:** תוקן עם סיווג טלפון/טאבלט/מחשב.

### 15. `📋` דיאלוגים מובנים לא אמינים ב-iOS Safari

**שורש:** `confirm()` מפעיל `window.blur` (שמדליק מנגנון פסילה) ומדוכא תחת `beforeunload`.

**מצב:** **אל תשתמש ב-`confirm()`/`alert()` בשום זרימה קריטית בצד הנבחן.** מודאל בתוך הדף בלבד.

### 16. `📋` אישור בוואטסאפ נשלח מהמספר הפרטי

**שורש:** ב-iOS אין דרך לכפות את אפליקציית ה-Business.

**מצב:** אנדרואיד ומחשב מטופלים. **iOS בלתי אפשרי — הוחלט, לא להציע שוב.** במחשב נדרש שיוך חד-פעמי ידני של web.whatsapp.com.

---

## ה. התנהגויות שנראות כמו באג ואינן

### 17. `✅` `Missing origin` בקריאת curl ל-API

התנהגות **תקינה** של ה-allowlist. הוסף `&origin=examinee-app`.

### 18. `✅` נבחן שנדחה או אופס לא ראה הודעה — תוקן 21/09 ערב (פריט 14 בסקירה)

**היה:** `rejected` דולג בכוונה (תקלת ת.ז. משותפת, 31/05), ולכן נבחן שנדחה או אופס ראה "לא נמצא רישום" ואחרי 3 סקרים "שגיאת שרת", ונתקע עד ניקוי הדפדפן. **עכשיו:** כשאין רישום חי, השורה **החדשה ביותר** מכריעה: `rejected` → "הבוחן דחה את הכניסה", `cancelled` → "ההרשמה בוטלה על ידי הבוחן" — שניהם עם כפתור "חזרה להרשמה" שמנקה את המכשיר; `completed`/`disqualified`/אין שורה → "לא נמצא רישום" כמו קודם. "החדשה ביותר" (ולא "הראשונה שהלולאה פוגשת") הוא מה ששומר על תקלת הת.ז. המשותפת בלתי אפשרית. השרת וה-Worker עונים זהה (בדיקת החוזים), ושניהם קוראים שוב מהגיליון לפני שעונים החלטה מעותק מטמון — כדי לא לדחות מישהו שבדיוק נרשם מחדש. עם הדחיפה של הבוחן ההודעה מופיעה ~1 שנ׳ אחרי הלחיצה.

### 19. `📋` צילום מסך אינו חסום בצד הנבחן

**הוחלט לא לבנות.** אי אפשר לחסום בדפדפן, ומנגנון היציאה-מהמסך כבר תופס שליחה מאותו מכשיר. מכשיר שני הוא באחריות המשגיח. לא להציע שוב ללא בקשה.

---

### לוח בוחן שלא מתעדכן דקות ארוכות ואז "הכל מגיע בבת אחת" (22/09/2026)

המחשב נרדם / המסך ננעל / הכרטיסייה ברקע, או ההוטספוט נפל. הבקשה הפתוחה של הלוח ל-Worker (`/v1/session/watch`) לא נשלחת כשהדפדפן לא רץ — בלוג של ה-Worker זה נראה כפער של דקות בלי אף בקשה מהמחשב, ואחרי ההתעוררות בקשה אחת שעונה "השתנה" והלוח מתמלא. לא תקלה של המערכת; הנוהל ב-OPERATIONS §5.

## ו. סביבת פיתוח (Windows + OneDrive)

### 20. `📋` קובץ חדש "לא קיים" עבור node/bash

קבצים **חדשים** שנוצרו בכלי עריכה עלולים להישאר placeholder של OneDrive ולא להיות נגישים ל-node/bash. עריכות לקבצים **קיימים** כן עוברות. לסקריפטים זמניים — כתוב ל-`/tmp` דרך heredoc.

### 21. `✅` עברית הופכת ל-`??????` בקונסולה

בכל `grep` על פרודקשן — בחר סמן ב-ASCII (שם משתנה), לא טקסט עברי.

---

## ז. פתוח / לא אומת

| נושא | מצב |
|---|---|
| ~~תרגול תלמידים: כרטיסיות + חזרה על שגיאות~~ | ✅ **נסגר 28/07/2026** — נבדק בקוד ושניהם נטענים מהשרת. נותר רק קוד מת (`TODO.md` §7) |
| iPhone שנתקע ב"ממתינים" | דווח, לא נחקר עד הסוף |
| תוצאה שנעלמה במחשב באחד האתרים | דווח, לא הוסבר |
| ~~טריגר `rebuildAtRiskCache`~~ | ✅ רץ 03:04 יומי (אומת 26/08/2026); מותקן מחדש ע"י `installNightlyJobs` |

---

## ח. סדר התשובות שונה בין השפות (20/09/2026)

### 22. `✅` אחרי החלפת שפה באמצע מבחן/תרגול, התשובה המסומנת "קפצה" לתשובה אחרת

**מה נראה:** תרגול C1 ב-20/09 — בעברית סומנה התשובה הנכונה, ואחרי מעבר לאנגלית הסימון ישב על תשובה אחרת ("The fan" במקום "The clutch").

**העובדה בנתונים (מפתח התשובות `answer_key.gs`):** עברית, רוסית ואמהרית מציגות את התשובות של כל שאלה באותו סדר. אנגלית, צרפתית, ספרדית וערבית — לכל אחת סדר משלה (הסכמה על אינדקס התשובה הנכונה עם כל שפה אחרת: ~26%, כלומר מקרי). הערבוב של התשובות נקבע פעם אחת לשאלה ומוחל על רשימת התשובות של השפה המוצגת, ולכן אחרי מעבר בין קבוצות סדר, המיקום המסומן מציג תשובה אחרת.

**הציון לא הושפע:** כל תשובה נשמרת עם השפה שבה ניתנה (`langAtAnswer`), והשרת מנקד אותה מול האינדקס הנכון של אותה שפה (`effectiveCorrectIdx`). גם בתרגול הניקוד נעשה ברגע הבחירה.

**התיקון (לקוח, שני הדפים):** שאלה שנענתה בשפה מקבוצת סדר אחרת מוצגת עם התשובות כפי שנראו כשענו עליה, עם הודעה בשפת הנבחן וכפתור "לענות מחדש בשפה הנוכחית" (מוחק את הבחירה ומציג את התשובות בשפה הנוכחית). מעבר בין עברית/רוסית/אמהרית לא נוגע בזה.

**מה יסגור את זה סופית:** יישור סדר התשובות של en/fr/es/ar לסדר העברי בקובצי התרגום (עבודה חד-פעמית של התאמה סמנטית, ~1,260 שאלות × 4 שפות). עד אז הכלל "אותו אינדקס" בתיקוני תרגום נשאר בתוקף לכל שפה בנפרד.

### 23. `✅` אמהרית: שתי שאלות מנוקדות הפוך (1442, 1443)

**תסמין:** נבחן אמהרי בדרגה C שבוחר את התשובה הנכונה בשאלות 1442 ("מהו רכב מורכב?") או 1443 ("הגדר רכב מחובר") נחשב טועה, ומי שבוחר את התשובה השגויה שבמיקום 3 נחשב צודק.

**שורש:** ב-28/03/2026 האמהרית נבנתה מחדש מ-`amharic_translations.xlsx` דרך `convert_amharic.py`. הקובץ סימן את התשובה הנכונה במיקום העברי; בשתי השאלות האלה סדר התשובות באמהרית השתנה והסימון לא זז איתו. מפתח התשובות של אמהרית זהה לעברי, ולכן השרת משחזר את הטעות. נמצא 20/09/2026 בבדיקת יישור סדר התשובות (כל 56 המועמדות משתי שיטות נקראו ידנית; רק שתיים אמיתיות).

**תיקון:** `deployment/generated/questions_am.json` תוקן מקומית ב-20/09 (סדר עברי הוחזר בשתי השאלות; גיבוי `questions_am.json.bak_preAmFix_2026-09-20`; המפתח נשאר 2). **נפרס 21/09/2026 06:50:** הקובץ הוחלף ב-Drive, `emergencyClearAndRefreshCache()` בנה מחדש 7/7 שפות ב-52 שניות (254 מפתחות, 0 כשלי פענוח), ושליפה חיה של 1442/1443 באמהרית אימתה שהאינדקס הנכון (2) מצביע על הטקסט הנכון בשתיהן. לבדוק ב'תוצאות' אם נבחן אמהרי קיבל אחת מהן ("מזהה שאלה: 1442/1443" בפירוט השגויות).

**הקשר:** מקור כל התרגומים ולמה הסדר שונה בין שפות — `docs/TRANSLATION_LINEAGE_2026-09-20.md`.

---

## ט. מהדורת r30 (נפרסה 21/09/2026 בערב, לפני בחינות 22/09) — מה השתנה ומה עלול להפתיע

**הרקע:** הבדיקה המלאה של 21/09 (`docs_private/reviews/2026-09-21/ (מחוץ לריפו הציבורי)/`) מצאה 40 ליקויים חיים ואת נקודת המפנה של האיטיות (15/05: השאלות עברו לשרת). התכן: `docs/DESIGN_2026-09-21.md` (§11 = ההחלטות הסופיות של יוסי מהערב: בנק פרטי דרך ה-Worker, סקר מהיר, רענון אוטומטי חוזר, פריסה באותו ערב).

### 24. `✅` "מאגר השאלות מתעדכן כעת" / `question_cache_busy` — לא קיים יותר

אין מטמון שאלות. התחלת מבחן = `startExam` (מזהים + סדר תשובות + אישור חתום), והטקסטים מגיעים מיד אחריה מה-Worker. `emergencyClearAndRefreshCache`, החימום וה-lease נמחקו. אם דף נבחן ישן (לפני הפריסה) מנסה `getExamQuestions` — השרת עונה `client_outdated` והדף מבקש רענון.

### 25. `✅` הבנק פרטי: הטקסטים מוגשים מה-Worker רק לשאלות שהונפקו

עד r30 `getQuestionsByIds` מסר טקסטים **ותשובות נכונות** לכל מבקש בלי אימות (נבדק חי 21/09). עכשיו: הטקסטים יושבים ב-`assets/` של `session-gateway` (לא בגיט, לא ב-Pages) ומוגשים רק לפי אישור חתום (HMAC עם `GATEWAY_KEY`): נבחן מקבל את 30 השאלות שלו בכל 7 השפות (`startExam` → `bank.grant` → `/v1/bank`), תלמיד עד 30 לשליפה, בוחן אישור בוחן (`bankGrant`) לחיפוש ולטבלת "שאלות שגויות". התשובות רק במפתח שבשרת ונמסרות לתרגול רק לסט שנשלף. נבחן במבחן לעולם לא מקבל `ci`. **חשיפה ידועה ומקובלת (יוסי, 21/09 ערב):** בוחן מחובר (ת.ז. + סיסמה) יכול עם האישור שלו להוריד את הבנק המלא בכל 7 השפות (~5.6 MB, בלי תשובות) — חיפוש התמונה וטבלת "שאלות שגויות" צריכים את זה; הוחלט לא לצמצם (TODO §6). **מה עלול להפתיע:** `startExam` עונה `bank_not_configured` כשחסר `GATEWAY_KEY`/`GATEWAY_URL`; אישור שנחתם במפתח אחר → 403 מה-Worker → "לא הצלחנו לטעון את השאלות" (הדף מנסה שוב לבד). `OPERATIONS.md` §5.

### 26. `✅` סקר הנבחנים דרך Worker: `stale:true` ו-`upstream_unavailable`

`checkApproval`/`getExamStatus` הולכים ל-`session-gateway` (מ-r33 גם ישירות לגוגל — רק מטלפון שלא מגיע ל-Worker, כל 12/20 שנ׳; #38) (בקשה אחת לסשן כל 2 שנ' לשרת; הנבחן סוקר כל 2–3 שנ' בהמתנה ו-6 שנ' במבחן). לוח הבוחן "דוחף" את ה-Worker (`POST /v1/invalidate`) אחרי כל החלטה **עם ההחלטה עצמה** (מזהה + סטטוס + דקות/שמע) — ה-Worker כותב אותה ל-snapshot מיד, כך שהנבחן רואה אישור/פסילה/הארכה בסקר הבא (~1 שנ׳ אחרי הלחיצה), והקריאה מהשרת שאחריה (≤2 שנ׳) מאשרת. הדחיפה נשלחת רק אחרי ש-Apps Script ענה `ok`, אז היא לעולם לא מקדימה את האמת, והיא מאומתת באישור הבוחן החתום (21/09 ערב): בלי אישור תקף ה-Worker דוחה אותה ב-403, כך שנבחן לא יכול "לפסול" חבר לכיתה מהטלפון. הארכת זמן נדחפת רק לנבחן שכבר במבחן (סטטוס `in_exam` מהלוח) — נבחן ממתין מקבל אותה בקריאה הרגילה. כשגוגל לא עונה ה-Worker מגיש עותק עד 60 שנ' עם `stale:true`, ואחריו `upstream_unavailable` — הדף **מאט** ולא נופל לסקר ישיר (אחרת סערת 16/09 חוזרת). **אין נפילה לסקר ישיר (יוסי, 21/09 ערב: "Cloudflare הוא הדרך, בלי 'מה עושים אם'"):** ה-Worker הוא המסלול היחיד של הסקר; כשהוא לא עונה הדף ממשיך לנסות אותו עם backoff (עד 20 שנ', 30–60 כשמנגנון "שרת מדרדר" נדלק) ומציג את שגיאת הסקר הרגילה אחרי 3 כשלים. Cloudflare למטה לזמן ארוך מטופל תפעולית (חזרה לאחור, #31), לא בדף. `checkApproval`/`getExamStatus` פרשו מה-API (21/09 ערב) — דף ישן מקבל 'גרסה חדשה — רענן'; הפונקציות נשארות כאמת-מידה לבדיקות החוזים. **סקר ארוך (21/09 ערב):** דרך ה-Worker הבקשה נשארת פתוחה עד 25 שנ' (`wait=25&fp=…`) ונענית ברגע שההחלטה נכנסת — בכלי הפיתוח זה נראה כבקשה "תלויה" עד 25 שנ' וזה תקין; בקשה שנהרגה בנעילת מסך לא נספרת ככשל של ה-Worker; Worker ישן מול דף חדש עונה מיד והדף חוזר לקצב 2–3/6 שנ'; שתי בדיקות חד-פעמיות (סוף הטיימר, אימות אחרי רענון) לעולם לא מוחזקות.

### 27. `✅` ארכוב לילי של `מבחנים` ו-`תוצאות`

`archiveSheets` (01:00) מעביר שורות ישנות מ-2/30 יום לארכיונים. שורה של אתמול שלא בגיליון החי — נמצאת בארכיון, לא אבדה. הריצה מדלגת כשיש סשן פעיל או רישום לא-סופי ב-3 השעות האחרונות.

### 27א. `✅` לוח הבוחן הציג נבחן "במבחן" מעל דקה אחרי ההגשה (חזרה 21/09 ערב) — תוקן: הלוח לא מאט את עצמו

גוגל ענה לאט (4–10 שנ׳ לקריאה) והלוח, לפי כלל מאוגוסט, ריווח את הסקר שלו עד 20 שנ׳ ועד 30–60 שנ׳ אחרי timeouts — רק רענון ידני הראה את התוצאה. מאז r30 הטלפונים לא פונים לגוגל, והלוח הוא בקשה אחת בכל פעם לבוחן, אז הכלל רק הזיק. **עכשיו (`steady`):** 5 שנ׳ אחרי כל תשובה, 2 שנ׳ כשתוצאה בדרך, 5 שנ׳ אחרי כשל — לא משנה כמה מהר גוגל ענה. מה שנשאר תלוי בגוגל: משך התשובה עצמה. באותו ערב תוקן גם ה-Worker: החזקה נענית תמיד עד wait+1 שנ׳ (נמדדה החזקה של 44 שנ׳ כשגוגל היה איטי).

### 27ב. `✅` r31 (לילה 21–22/09): לוח הבוחן בלי טיימר, המכשיר דוחף אחרי הגשה, הסקר מחזיק מיד

ההמשך של 27א — יוסי (הודעות 48–51): "למה 5 שניות ולא 0". הלוח לא סוקר יותר: בקשה אחת פתוחה ל-`/v1/session/watch` ב-Worker (אישור בוחן חתום; טביעה של כל שורות הסשן: סטטוס, שמע, דקות, הארכה, אזהרות, "סיים במכשיר", מסך נוסף, פסילות) שנענית ברגע שמשהו השתנה, ורק אז `examinerDashboard`. הצפייה לא עולה לגוגל כלום נוסף (ה-Worker ממילא מרענן את ה-snapshot כל ≤2 שנ׳ כל עוד מישהו מחזיק). **מה עלול להפתיע:** (1) בכלי הפיתוח של הבוחן רואים בקשה "תלויה" עד 25 שנ׳ ל-Worker — תקין; (2) קריאה מלאה נעשית גם בלי שינוי אחת ל-60 שנ׳ (רשת ביטחון לשינויים שלא עוברים ב-snapshot: תיקון תוצאה מדף אחר, השלמה אוטומטית של שורה תקועה); (3) אם ה-Worker לא עונה — הלוח חוזר ל-5 שנ׳ כמו ב-r30 ומתאושש לבד; (4) אישור בוחן שפג (8 שעות מהכניסה) → 403 → הדף מבקש אישור חדש לבד. **דחיפה מהמכשיר:** אחרי הגשה שאושרה (ו-2 שנ׳ אחרי "סיימתי") הטלפון שולח `POST /v1/invalidate?sessionCode&idNumber&examineeToken` — ה-Worker מאמת את גיבוב הטוקן מול השורה ורק **מוחק** את ה-snapshot (לעולם לא כותב לתוכו), כך שהלוח רואה את התוצאה בקריאה הבאה (~0.7 שנ׳). **הסקר של הנבחן:** תשובה שנושאת `fp` מתזמנת את הבקשה הבאה אחרי 250 ms גם כשלא הוחזקה — ההחזקה מתחילה מיד ולא אחרי 2–6 שנ׳; Worker ישן (בלי `fp`), `stale` וכשלים שומרים על הקצב הישן. **בשרת:** `markPendingCompleted` (ההגשה) לא ביטל את ה-snapshot של 4 השניות — תוקן ב-r31 (עד ההדבקה: התוצאה יכולה להגיע ל-Worker עד 4 שנ׳ מאוחר יותר).

### 27ג. `✅` r31.1 (22/09 בוקר): החזקה של 45 שנ׳, ביטולי "hung" של Cloudflare, ותנודת טביעה — נמצאו בלוג החי ותוקנו

`wrangler tail` בזמן שלוח בוחן היה פתוח (05:22–05:46) הראה: (1) בקשות watch שנמשכו 27–45 שנ׳ במקום ≤26 — הקריאה הראשונה מגוגל (שהצטרפה לקריאה שהחזקה קודמת נטשה) לא הייתה מוגבלת ורק אחריה התחילה ההחזקה; הדף מוותר ב-40 שנ׳ וסופר כשל. (2) 15 בקשות שנענו 500 תוך 3 ms: "The Workers runtime canceled this request because it detected that your Worker's code had hung" — בקשה שמחכה להבטחה של בקשה אחרת (ההבטחה של importKey, ואחריה הקריאה המאוחדת) בלי I/O משלה. (3) שתי טביעות לאותו סשן מתחלפות כל 250 ms: snapshot בצורת r30 (בלי warn/fin/ext/dq → '') מול r31 (0 → '0') חיים בו-זמנית בזיכרון ובמטמון, וכלל "ענה עם העותק ששונה ממה שהלקוח מחזיק" הקפיץ ביניהם. **התיקון (r31.1, version b6bdbc2e):** תקציב אחד לבקשה מרגע ההגעה (wait+1 שנ׳; 20 שנ׳ בלי wait) שהקריאה הראשונה וההחזקה חולקות; כל המתנה על הבטחה משותפת רצה מול טיימר של הבקשה עצמה (`within`); המפתח נשמר ולא ההבטחה; הטביעה נחתמת פעם אחת ב-snapshot (`sfp`, גם במטמון) — 8 גיבובים להחזקה במקום 25 (CPU 7–12 ms → פחות); חסר ו-0 מגובבים זהה; שורת לוג מובנית לכל תשובה (`src`, `fp`, `usfp` — `fp !== usfp` = תנודה). **מה עלול להפתיע:** עד 60 שנ׳ אחרי הפריסה סשן יכול עוד להראות שתי טביעות (עותקים ישנים במטמון); Google שלא עונה תוך התקציב → `stale:true` או `upstream_unavailable` (הדף מנסה שוב, הקריאה הנטושה נוחתת במטמון).

### 28. `✅` בוחן/מורה: באנר ורענון עצמי כשבטוח; `version.json` במקום ETag

באנר + "רענן עכשיו", ורענון עצמי אחרי 60 שנ' רק כשאין מודאל פתוח ואין פעולה בהמתנה (נבדק שוב כל 15 שנ'). יוסי החליט (21/09 בערב) שהדפים חייבים להתעדכן לבד; הלולאה של 16/09 לא יכולה לחזור כי `version.json` נושא hash של תוכן הדף — דחיפה שלא שינתה את הדף לא מציגה כלום. `tokenExpired` בסקר לא מנתק יותר — נדרש אימות חוזר (`verifyLogin`) לפני באנר "התחבר מחדש". **כלל תפעולי:** לא דוחפים ל-Pages בזמן בחינות; ואם חייבים — הרענון יגיע לבוחנים תוך ~3–4 דקות אחרי הדחיפה, לכן לבדוק את הגרסה החדשה קודם.

### 29. `✅` פסילה במחשב: חסד 2 שניות

עד 22/09 אירוע `hidden` ראשון במחשב פסל מיד (התיעוד טען 2 שנ' — היה קוד מת). עכשיו באמת 2 שנ' חסד, 0 אזהרות. טלפון ללא שינוי.

### 30. `✅` דו"ח משותף לאתר נפל ב-500 כשעמודת התאריך היא טקסט

**תסמין:** "שגיאה בטעינת דו"ח משותף" לסשן מסוים, בלי קשר לעומס.

**שורש:** `handleSiteCombinedReport` הריץ `new Date("DD/MM/YYYY HH:MM").toISOString()` על תאריך שנשמר כטקסט → `RangeError: Invalid time value` שהפיל את כל הדו"ח. נמצא בבדיקה של 21/09/2026 (סוכן S2) בזמן המעבר ל-`readResultsSince`.

**מצב:** תוקן במהדורת r30 (22/09/2026) — התאריך עובר דרך `parseSheetDateTime`.

### 31. `⚠️` ה-Worker (Cloudflare) הוא תלות קשה של התחלת מבחן — במקום Drive של Google

מאז r30 הטקסטים מגיעים רק מ-`session-gateway`, ומאז 21/09 ערב גם הסקר עובר רק דרכו (אין נפילה לסקר ישיר — החלטת יוסי: Cloudflare הוא הדרך; עמידות ל-Cloudflare-למטה נדחתה, TODO 4.3). Cloudflare למטה = אי אפשר להתחיל מבחן, ונבחן שממתין או במבחן לא מקבל אישור/פסילה/הארכה עד שה-Worker חוזר (מבחן שכבר התחיל ממשיך מקומית ומוגש לשרת כרגיל; גם השמע כבר תלוי ב-Cloudflare מאז ומעולם). עד r30 אותה תלות הייתה ב-Drive של Google (19/08, 14/09 היו נפילות Google). **מה לעשות:** `curl -s https://session-gateway.bohanyzahal.workers.dev/` ו-https://www.cloudflarestatus.com/; אם ה-Worker לא חוזר תוך דקות — חזרה מלאה ל-r24 לפי `DEPLOY_2026-09-22.md` §8 (השרת הישן מגיש שאלות בעצמו). **לא לרוקן את `GATEWAY_URL`** כפתרון — זה מכבה גם את הטקסטים. תקציב: 100k בקשות/יום לחשבון (סקר + בנק + TTS + תמונות); מעל התקציב Cloudflare מחזיר שגיאה 1027 עד 00:00 UTC — הסקר והבנק נעצרים יחד. עם הסקר ארוך (DESIGN §12) הצפי ~100 בקשות לנבחן (400 נבחנים ≈ 40–70k); ביום גדול לבדוק את המונה בלוח Cloudflare בצהריים.

### 33. `⚠️` r31: שני פרויקטי Apps Script — `wrong_deployment`, מאפיינים כפולים, טריגרים בפרויקט הקר

הקובץ המודבק גדל ל-565 KB וגוגל מקמפל אותו בכל בקשה (1.2 שנ׳ מתוך 2.7 בפעולה ריקה — נמדד 21/09 מול סקריפט בקרה של 10 KB באותו חשבון). r31 מייצר שלושה קבצים (DESIGN §13.3; ההפעלה ב-`DEPLOY_2026-09-22.md` §11): המונוליט הדחוס (~330 KB), `.exam.js` (~170 KB, הפרויקט הקיים) ו-`.reports.js` (פרויקט **עצמאי** חדש: דו"חות, לוח מפקד, חיזוי, מורים, תרגול, ניהול, ארכוב לילי). הדפים מנתבים לפי פעולה (22 פעולות "קרות" ב-`ExamTransport.REPORTS_ACTIONS`). **מה עלול להפתיע:** (1) פעולה שהגיעה לפרויקט הלא-נכון עונה `{code:"wrong_deployment"}` — דף ישן שנשאר פתוח אחרי ההפעלה יראה את זה בפעולת דו"ח עד שהוא מתרענן לבד (דקות); (2) לפרויקט הקר יש CacheService ומגבלות קצב משלו — הגבלת קצב של התרגול לא משתפת מונה עם המבחנים, וזה בסדר; (3) הפרויקט הקר פותח את גיליון הבחינות לפי `EXAM_SPREADSHEET_ID` — חסר/שגוי → כל פעולת דו"ח נכשלת עם הודעה ברורה; (4) הטריגרים הליליים חייבים לרוץ בפרויקט הקר בלבד (`installNightlyJobs` שם, `uninstallNightlyJobs` בקיים) — טריגר שנשאר בפרויקט הקיים אחרי הדבקת `.exam.js` ייכשל כל לילה על פונקציה חסרה; (5) `answer_key.gs` חייב להיות בשני הפרויקטים (התרגול מנקד ממנו). עד ההפעלה (`REPORTS_API_URL === API_URL` בדפים) הכל הולך לכתובת אחת והמונוליט מגיש הכל — אין שינוי התנהגות.

### 34. `✅` r31.2 (22/09 בוקר): הרשמה שגוגל ענה לה באיחור → "כבר רשום" בלי טוקן → "טוקן נבחן לא תקין" בכל התחלת מבחן

**תסמין (נראה חי 08:35 באתר הבדיקה, בזמן תקיעות של Google):** הנבחן נרשם, ההרשמה "נכשלה" בטלפון (פג הזמן אחרי 30 שנ׳), הוא לחץ שוב, השרת ענה "כבר רשום בסשן זה", הדף המשיך למסך ההמתנה **בלי טוקן**, הבוחן אישר (ה-Worker עונה לסקר גם בלי טוקן), ו"התחל מבחן" נכשל שוב ושוב עם "טוקן נבחן לא תקין" (reason: missing). ביום בחינות זה נראה כמו "המבחן לא מתחיל". **התיקון:** הדף שולח `regKey` (מפתח אקראי לניסיון הרשמה, נשמר בין הניסיונות וברענון), השרת זוכר `regKey → טוקן` ב-CacheService ל-30 דקות ומחזיר לאותו מפתח את הטוקן של השורה הקיימת (`resumed:true`); מפתח אחר או דף ישן בלי מפתח עדיין מקבלים "כבר רשום". נעילת סקריפט best-effort (5 שנ׳) סוגרת גם ריצה כפולה אמיתית. ההרשמה מחכה עכשיו 60 שנ׳ (לא 30). **מה עלול להפתיע:** אם המטמון של גוגל שכח את המפתח (מעל חצי שעה, או פינוי) — "כבר רשום" כמו קודם; הפתרון הידני נשאר: הבוחן לוחץ **אפס** והנבחן נרשם מחדש. דורש את הדבקת `.exam.js` העדכנית (הדף לבדו לא מזיק מול שרת ישן).

### 35. `⚠️` 22/09/2026: תשובות Apps Script מגיעות באיחור של 25–60 שנ׳ או 404 — בצעד המסירה של גוגל, לא בהרצה, ורק לפרויקטים שלנו

**מה נמדד (22/09 08:20–11:15, מהמחשב של יוסי):** בקשה ל-`/exec` מתפצלת לשני צעדים: (1) `script.google.com/…/exec` — מריץ את הקוד ועונה 302; (2) `script.googleusercontent.com/macros/echo?user_content_key=…` — מוסר את הפלט. אצלנו צעד 1 לוקח **1.0–1.5 שנ׳ תמיד** (וה-Executions מראים 0.6–4 שנ׳ להרצה), וצעד 2 לוקח **0.4 שנ׳ או 25–30 שנ׳, לפעמים 404**, עם שרשרת 302 שקופצת בין googleusercontent ל-script.google.com. סקריפט בקרה של 10 KB באותו חשבון: צעד 2 תמיד 0.3–0.4 שנ׳. זה פוגע בשני הפרויקטים שלנו (מבחנים ודו"חות) וגם בפריסה הישנה מפברואר; פריסת "Worker" נפרדת של אותו פרויקט לא עזרה (הוסרה). ה-Worker מקלאודפלר רואה את אותו דבר (קריאות snapshot של 2–18 שנ׳). ניסיון חוזר של צעד 2 עם timeout קצר לא מציל באופן אמין (3 timeouts רצופים נמדדו). **מסקנה:** צד גוגל, ברמת הפרויקט/החשבון שלנו, לסירוגין (גם 20/09 07:12–07:14 עם r24). **מה זה עושה למערכת:** כל פעולה שכותבת בגיליון (הרשמה, אישור, התחלה, הגשה, רשימת הלוח) יכולה לקחת עד דקה בזמן גל; ה-Worker עונה מעותק ישן/`upstream_unavailable` בתוך התקציב והדפים מנסים לבד; הרשמה חסינה לניסיון חוזר (#34). **מה לעשות בזמן גל:** לא לרענן, לא ללחוץ שוב, לא לפרוס; ווטשדוג: `outside ms` גדול = זה. **המתכון "אנחנו או הם":** `curl -s -o /dev/null -D - "<exec>?action=health&origin=examinee-app"` → Location → `curl -s -o /dev/null -w "%{http_code} %{time_total}" "<Location>"`: צעד 1 מהיר וצעד 2 איטי = מסירה של גוגל. **מה עשינו בעקבות זה (r31.3, 22/09 12:00–13:10):** ה-Worker קורא מגוגל רק אחרי הודעה (כל כתיבה של בוחן/נבחן מודיעה) + קריאת ביטחון כל 20 שנ׳ לסשן מוחזק (פי 10 פחות הרצות: 29 במקום ~300 ל-41 בקשות ב-10 דק׳); הלוח שולח drop גם כשהחלטה לא אושרה בזמן (12:29: האישור נקלט בגוגל אחרי שהדף ויתר, והטלפון חיכה לקריאת הביטחון). ההימור: פחות הרצות = פחות תקיעות במסירה. אם לא — המצב החי חייב לצאת מגוגל.

### 36. `✅` 22/09 בצהריים: קישור עם קוד של סשן חדש לא ניצח מצב שמור של סשן ישן באותו טלפון

**תסמין:** הבוחן שלח את הקישור של הסשן של היום; בטלפון שנבדק בבוקר (סשן שנסגר) הדף שחזר את המצב השמור והמשיך להירשם לקוד הישן, גם אחרי "חזרה להרשמה" וגם מקישור טרי. סיבה: המילוי מהקישור (`?code=`) רץ רק כשאין מצב משוחזר. **תיקון (r31.3, examinee.html):** קישור שמביא קוד **אחר** מהמצב השמור גובר עליו, אלא אם יש מבחן באמצע (מקומי, השעון רץ — לעולם לא נמחק בגלל קישור ישן בשורת הכתובת); אותו קוד — שחזור כרגיל. **עקיפה עד שהדף מתעדכן בטלפון:** לפתוח את הקישור בכרטיסייה פרטית. שכיח בבדיקות; אמיתי גם לנבחן חוזר באותו יום מסשן שנסגר.

### 37. `📋` r32 (נבנה 22/09/2026 בערב, **טרם נפרס** — לפרוס רק אחרי יום הבחינות 23/09): הלוח מצטייר מה-Worker, ההרשמה רצה ברקע — מה עלול להפתיע

**מה השתנה (DESIGN §14):** (1) תשובת ה-watch של ה-Worker נושאת את השורות והתוצאות של הסשן (`session`), והלוח מצייר מהן — `examinerDashboard` נקרא רק בפתיחה, ברשת הביטחון (60 שנ׳), אחרי החלטה של הבוחן, ובלחיצה שצריכה את פירוט השגויות; (2) אחרי "הירשם" הטלפון עובר מיד למסך ההמתנה, וההרשמה עצמה נשלחת ברקע עם ניסיונות חוזרים אוטומטיים לאותו `regKey` (30 שנ׳ לניסיון, 3/6/12/20 שנ׳ בין ניסיונות); (3) `registerExaminee` מסרב לסשן שלא קיים / נסגר / פג (TODO 1.5); (4) ניסיון חוזר חופף לאותו `regKey` מייצר שורה אחת (תביעה במטמון לפני הכתיבה).

**מה עלול להפתיע:**
- **"פירוט שגויות" נטען בלחיצה.** תוצאה שהגיעה מה-Worker אינה נושאת את `wrongDetails`; דו"ח לנבחן / דו"ח משותף / טבלת השגויות / שליחה מרוכזת עושים קודם קריאת `examinerDashboard` אחת (2–5 שנ׳, או יותר בגל של גוגל) ורק אז נפתחים. תוצאה שכבר נקראה פעם (הפתיחה, רשת הביטחון) נפתחת מיד.
- **"נשלח" שמהבהב:** סימון "נשלח" נכתב לגיליון ומודיע ל-Worker (drop); בגל של גוגל ייתכן רינדור אחד עם הסימון הישן עד שהקריאה החוזרת מגיעה.
- **שורה תקועה ב"במבחן" נסגרת תוך דקה, לא תוך 5 שנ׳:** ההשלמה האוטומטית (timeout → נכשל) רצה בתוך `examinerDashboard`, שנקרא עכשיו כל 60 שנ׳.
- **הנבחן רואה "ממתין" לפני שהבוחן רואה אותו** — זה מכוון. שורת המצב מתחת לכותרת אומרת איפה ההרשמה: "שולח…" → "ההרשמה הגיעה לבוחן ✔ מחכים לאישור ההרשמה מהשרת…" → "נרשמת ✔". כפתור "התחל מבחן" נדלק רק כשגם התיבה מסומנת וגם אישור ההרשמה (הטוקן) הגיע — אושר בלי טוקן = הודעה מתחת לכפתור, לא "טוקן נבחן לא תקין".
- **"כבר קיים רישום לת.ז. זו"** במסך ההמתנה = השרת ענה "כבר רשום" בלי טוקן (מכשיר אחר, מטמון שנשכח, שרת ישן): הנבחן לא יוכל להתחיל; הבוחן לוחץ **אפס** והנבחן נרשם שוב. זה מה שהיה "טוקן נבחן לא תקין" ב-#34.
- **Worker חדש מול שרת ישן / דף ישן:** בדיוק r31.3c (אין `session`, הטביעה זהה). שרת חדש מול דף ישן: הדף מתעלם. אפשר לפרוס כל רכיב לבד (סדר מומלץ: Worker → שרת → Pages).
- **`nr:0` תמיד ב-`wrangler tail` בסשן עם מבחנים גמורים** = השרת עדיין r31 (ההדבקה לא נעשתה).

**נפרס 22/09/2026 15:00–16:30 (החלטת יוסי, למרות יום הבחינות למחרת), ומה החזרה הראשונה לימדה (15:25):** (1) הלוח אכן צויר מה-Worker בכל שלב (`sess:1`); (2) **הדחיפה אחרי ההרשמה נדחתה ב-403** — ה-Worker בדק את הטוקן מול עותק שעוד לא הכיל את השורה, ולכן הלוח והטלפון חיכו לקריאת הביטחון (20 שנ׳ + קריאת גוגל של 12 שנ׳). **r32.1 (Worker, build `2026-09-23.1`):** דחיפה של ת.ז. שהעותק אינו מכיר = drop בתקציב `REREAD_GAP_MS` (כמו דחיפה ריקה של בוחן), לא סירוב; טוקן שגוי לשורה קיימת נשאר 403; (3) התוצאה הגיעה ללוח ~30 שנ׳ אחרי "סיים" (קריאת גוגל אחת של 24 שנ׳ — גוגל היה איטי כל אחר הצהריים, 4–25 שנ׳ לקריאה), אבל **לטלפון** האישור הירוק הגיע שתי דקות אחר כך — צעד המסירה של גוגל לתשובת `submitResult` (#35). **r32.1 (דף הנבחן):** מרגע שליחת התוצאה הטלפון מחזיק גם סקר סטטוס ב-Worker; `completed` בשורה מציג "התוצאה התקבלה" מיד (UI בלבד — העותק המקומי והניסיונות החוזרים של ה-POST נשארים עד שהשרת עונה, כי `completed` יכול להיות גם תוצאה מפוברקת של סיום כפוי/timeout).

**חזרה שנייה (15:54, אחרי r32.1 בשלושת הרכיבים):** הרשמה → לוח ~25 שנ׳, "סיים" → לוח ~50 שנ׳, → טלפון ~24 שנ׳. הלוג: הדחיפה אחרי ההרשמה התקבלה (200, drop) ✓, הלוח צויר מה-Worker בכל שלב (`sess:1`) ✓ — וכל השניות האלה הן קריאות `sessionSnapshot` של ה-Worker מגוגל ש**נכשלו או תקעו** (timeout 25 שנ׳, ואז 11 שנ׳; 12:54:51–12:55:58 UTC אף קריאה לא חזרה). בבוקר אותו יום אותה קריאה לקחה 0.7 שנ׳. מה שנשאר לשפר אצלנו קטן (TODO 0א.8); הזמן הוא של גוגל.

### 38. `⚠️` יום הבחינות 23/09/2026: באתר הגדול (60+ נבחנים, קליטה חלשה) רוב הנבחנים לא נכנסו למבחן הדיגיטלי ועשו מבחן בכתב — r33 (24/09) מוסיף מסלול גיבוי; הסיבה השורשית עדיין לא הוכחה

**החקירה (23/09 ערב, `docs_private/incidents/2026-09-23/`):**
- **נבחני "רפאים":** לפחות 13 נבחנים בשלושה אתרים (לא רק באתר הגדול) נרשמו דרך גוגל — השורה בגיליון, הבוחן אישר/איפס אותם עד 5 פעמים — אבל **אף בקשה שלהם לא הגיעה ל-Worker**. בינתיים שני אייפונים מחוברים חיכו שעה שלמה ולא אושרו: ללוח אין דרך להבדיל בין טלפון מחובר לשורת רפאים.
- **מי שהגיע ל-Worker התחבר כמעט תמיד ב-IPv6.** כלומר בכתובת אישית לטלפון: 18 מתוך 21 באתר הגדול; בפרטנר 49 מכשירים ב-IPv6 ואפס ב-IPv4.
- **ספארי מול כרום:** לפי יוסי ספארי (גם בגלישה פרטית) נכשל, וכרום באותו אייפון עבד. 11 אייפונים בספארי כן עבדו.
- **הסבר מוביל (לא הוכח):** Cloudflare חוסמת חלק מהלקוחות ב-`workers.dev` לפני שה-Worker רץ, ובקשה חסומה לא מופיעה ב-`wrangler tail`. בספטמבר 2026 יש גל דיווחים על 403/1010 בכתובות `workers.dev`, בלי שליטה לבעלים.
- **מה נשלל:** חסימת עוקבים של ספארי, Safe Browsing, רשימות חסימה של ספקים וחסימה של ספק מסוים. ל-Worker (ורק לו) יש רשומת DNS עם `alpn=h3` ו-`ech=`; לפי הבדיקה ספארי שולח את שם השרת גלוי, ולכן ECH נראה לא רלוונטי.
- **גם מורים מדווחים** שתלמידים בקבוצה גדולה במקום אחד מתקשים להיכנס לתרגול כבר כמה ימים. ב-Worker כל בקשות התרגול שהגיעו הצליחו, כך שמי שנתקע נתקע לפני כן.
- **פסילות שווא באייפון:** שלושה אייפונים נפסלו 15–45 שנ׳ אחרי התחלה, ואחד מהם שלוש פעמים אחרי כל ביטול. הסיבה: ניטור "מסך מפוצל" לפי יחס שטח, בשילוב הגדלת תצוגה בספארי (aA) מ-115% ומעלה. באייפון אין מסך מפוצל לספארי.

**r33 — נפרס ב-23/09 בערב** (השרת ~17:17, `2026-09-24-r33`; Pages `98ba689` ב-17:34). לפני הפריסה נעשו חזרות מול השרת החי: מסלול גוגל מלא ומסלול Worker מלא. אחריה נבדק הדף החי בשני המסלולים. **מה נכנס (DESIGN §15):**
- **מסלול גיבוי דרך גוגל** לטלפון שלא מגיע ל-Worker: אישור דרך `checkApproval` כל 12 שנ׳, סטטוס דרך `getExamStatus` כל 20 שנ׳, והשאלות דרך `bankRelay`, שבו השרת מביא מה-Worker את אותה תשובה עם אותה הרשאה. הטלפון מציג שורה ירוקה "📡", ובלוח מופיע תג "📡".
- **בדיקת חיבור ברקע** כבר במסך הקוד.
- **חזרה ל-Worker** כשהוא עונה שוב.
- **אבחון מהטלפון** (דפדפן, סוג השגיאה) לגיליון האבחון. בפעם הבאה נדע מה חוסם.
- **תיקון הפסילות באייפון**, וסיבת פסילה ששמורה בשורה.
- **תיקון ה-SW של התרגול:** הוא כבר לא לוקח בקשות לשרתים אחרים.
- **בדיקה לפני שסומכים על הגיבוי:** `testGatewayReachability()` בעורך של גוגל. אם גוגל עצמו לא מגיע ל-Worker, הממסר לא יעבוד.

**מה קרה:** באתרים הקטנים המבחנים עבדו, לאט. באתר הגדול רוב הנבחנים נתקעו ("בעיית תקשורת", "לא מקבלים מבחן אחרי אישור"), הבוחן איפס ואישר שוב ושוב, ובסוף רובם עשו מבחן בכתב. **מה ידוע מהלוג של ה-Worker (`wrangler tail`):** (1) מתוך ~50 שורות רישום בסשן הגדול רק 21 נבחנים שלחו אי פעם בקשה ל-Worker (18 אושרו, 17 התחילו) — לחלק מהנבחנים ההרשמה הגיעה לגוגל אבל **אף בקשה לא הגיעה ל-`workers.dev`**; זו השאלה המרכזית של החקירה. (2) ה-Worker עצמו תרם: בקשות מוחזקות צרכו 15–39 ms CPU מול תקרת 10 בתוכנית החינמית — 19 נקטעו ב-`exceededCpu` (503) בחצי שעה; וכל דחיפה מחקה את כל העותקים, כך שבזמן גוגל איטי כל הבקשות ענו `upstream_unavailable`. (3) גוגל: קריאות `sessionSnapshot` בחציון 4–6 שנ׳ עד 25, ותקיעות של 1.5–5 דקות.

**מה נפרס באמצע היום (Worker בלבד, בלי רענון דפים):** **r32.2** (10:27, build `2026-09-23.2`): drop שומר את העותק הקודם כגיבוי `stale` (לא נכתבים לתוכו טלאים), טיק ההחזקה 1→2 שנ׳, ענף הזיכרון פותח את גוף המטמון רק כשכותרת `X-RAT` אומרת שהוא **חדש יותר** ואז מאמץ אותו — CPU בחציון ירד ל-5–7 ms, אפס 503. **r32.3** (11:30, build `2026-09-23.3`, באישור יוסי): עותק ישן מוגש עד 180 שנ׳ (`STALE_SERVE_MS`; טלאי עדיין רק לעותק של עד 60 שנ׳ — `STALE_MS`), כדי שבתקיעה של גוגל לוח הבוחן לא ייפול לקריאת `examinerDashboard` כל 5 שנ׳. **עדיין פתוח:** למה חלק מהטלפונים לא מגיעים ל-Worker בכלל; הרשמה-מחדש אחרי איפוס — הדחיפה נדחית 403 והסקר עונה `a:tok` לכמה שניות; טלאי של בוחן לא עובר בין colos של Cloudflare (Cache API הוא לכל מרכז נתונים).

### 32. `✅` תרגול בבית (בלי קוד כיתה) היה נחסם ב"יותר מדי בקשות" — באג ב-r30 שנתפס בסקירה לפני הפריסה

**מה היה:** שרת r30 (21/09) הכניס מגבלות קצב ל-`startPractice`. מי שאין לו קוד כיתה נפל לדלי **אחד משותף** של 5 התחלות בדקה לכולם ביחד, ובדף התרגול קוד הכיתה אופציונלי — כלומר רוב התרגול בבית. בערב עמוס רוב המתרגלים היו מקבלים "יותר מדי בקשות. נסה שוב בעוד X שניות". **לא היה חי** — נתפס בסקירת הפריטים בערב 21/09 (פריט 9) לפני הפריסה.

**עכשיו:** מתרגל בבית מזוהה לפי מזהה התלמיד שהדף יוצר (`home:<studentId>`) ומקבל 20 התחלות בדקה משלו, כמו תלמיד בכיתה; מבחן עצמאי 5/דקה לת.ז.; הדלי המשותף (5/דקה) נשאר רק למי שאין לו שום זיהוי (סקריפטים); ותקרה אחת של 300/דקה לכל מי שבלי כיתה יחד — מגינה על Apps Script מהצפה. **מה שהמגבלות לא עושות:** להגן על הבנק — תרגול מחזיר תשובות (`ci`) מעצם טבעו, וסורק סבלני יכול להמציא מזהים; הבנק העברי עם התשובות ציבורי ממילא (משרד התחבורה), מה שרק אצלנו הוא התרגומים. באותו תיקון: `submitPracticeResult` שומר קוד כיתה רק אם הכיתה קיימת והתלמיד רשום בה (TODO 1.2 נסגר).
