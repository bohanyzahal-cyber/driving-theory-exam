# מודל הנתונים — Google Sheets

בסיס הנתונים של המערכת הוא שני גיליונות Google Sheets (בחינות; תרגול ומורים — מאז r24, 19/09/2026) ובהם 14 לשוניות + שלושה ארכיונים ליליים. אין schema ואין מיגרציות — **מיקום העמודה הוא החוזה**.

---

## 0. שלושה כללים לפני שנוגעים

### כלל 1 — קריאה מ-0, כתיבה מ-1

```js
var audio = data[i][9];                    // קריאה: מערך, אינדקס 0
sheet.getRange(i + 1, 10).setValue('on');  // כתיבה: getRange, אינדקס 1
```

**אותה עמודה, שני מספרים.** זה מקור הבאגים מספר 1 בקוד הזה. תמיד ודא מול הטבלאות למטה.

### כלל 2 — אסור להזיז עמודות

הקוד לא קורא לפי שם כותרת אלא לפי מספר. הוספת עמודה באמצע, מיון ידני של הגיליון, או מחיקת עמודה "מיותרת" — כל אחד מאלה שובר את המערכת בשקט. **עמודה חדשה נוספת רק בסוף.**

### כלל 3 — `SHEET_HEADERS` אינו מקור אמת

הקבוע ב-`server/src/00_config.js` הוא **תבנית ליצירת גיליון חדש**. מאז 22/09/2026 הוא מיושר לשימוש בפועל (`ממתינים` 19, `מורים` 10, `כיתות` 8) ובדיקה (`server_cost`) מוודאת שאורכו ≥ האינדקס המקסימלי שהקוד קורא.

הטבלאות במסמך הזה משקפות את מה שהקוד **באמת** קורא וכותב.

---

## 1. `ממתינים` — מצב האמת של הנבחן

הגיליון החם ביותר. כל דבר שקורה לנבחן בין ההרשמה לסיום נמצא כאן.

| עמודה | אינדקס קריאה | getRange | שדה | הערות |
|---|---|---|---|---|
| A | 0 | 1 | קוד סשן | |
| B | 1 | 2 | ת.ז. | תמיד דרך `normalizeId()` |
| C | 2 | 3 | שם | |
| D | 3 | 4 | טלפון | |
| E | 4 | 5 | זמן הרשמה | |
| F | 5 | 6 | **סטטוס** | ראה מכונת המצבים למטה |
| G | 6 | 7 | שפה | |
| H | 7 | 8 | אוכלוסייה | |
| I | 8 | 9 | דרגה | בחירת הנבחן, יכולה להיות שונה מהסשן |
| J | 9 | 10 | **שמע** | `on`/`off` — פר-נבחן מאז 28/07/2026 |
| K | 10 | 11 | הארכת זמן | `''` / `1.25` / `1.5` (מכפיל) |
| L | 11 | 12 | התחלת מבחן | |
| M | 12 | 13 | טוקן נבחן | נוצר בהרשמה, נדרש בכל קריאה עוקבת |
| N | 13 | 14 | ספירת DQ | |
| O | 14 | 15 | מסך נוסף | `כן` = זוהה מסך חיצוני |
| P | 15 | 16 | ספירת אזהרות | ⚠️ לא ב-`SHEET_HEADERS` |
| Q | 16 | 17 | אזהרה אחרונה | ⚠️ לא ב-`SHEET_HEADERS` |
| R | 17 | 18 | אתר | האתר שהנבחן בחר (מארח/אורח) |
| S | 18 | 19 | סיים במכשיר | הכותרת נכתבת אוטומטית בשימוש הראשון |

### מכונת המצבים (עמודה F)

```
waiting ──approve──> approved ──start──> in_exam ──submit──> completed
   │                    │                    │
   │                    │                    ├──anti-cheat──> disqualified
   │                    │                                          │
   │                    │                        ┌──overturn───────┤
   │                    │                        ▼                 ▼
   │                    │                    in_exam        dq_confirmed
   ├──reject──> rejected
   └──cancel──> cancelled
```

**עקרונות שאסור לשבור:**
- `disqualified` = פסילה שממתינה להכרעת בוחן. הרשמה מחדש **חסומה** במצב הזה (אחרת נוצרות שתי שורות ואותו נבחן מופיע פעמיים ב"במבחן כרגע").
- ביטול פסילה חייב לטפל גם ב-`disqualified` וגם ב-`dq_confirmed`.
- `rejected` מדולג בבדיקת האישור בכוונה — ראה `KNOWN_ISSUES.md`.
- הקריאה תמיד **מהשורה האחרונה אחורה**, כי אותה ת.ז. יכולה להופיע כמה פעמים (ניסיון חוזר).

---

## 2. `סשנים` — חלון בחינה

| עמודה | קריאה | getRange | שדה |
|---|---|---|---|
| A | 0 | 1 | קוד (6 תווים) |
| B | 1 | 2 | בוחן ת.ז. — **הבעלים**, רק הוא רשאי לעדכן |
| C | 2 | 3 | שם בוחן |
| D | 3 | 4 | אתר (מארח) |
| E | 4 | 5 | כיתה |
| F | 5 | 6 | דרגה |
| G | 6 | 7 | שפה |
| H | 7 | 8 | מצב שמע — **ברירת מחדל בלבד** מאז 28/07/2026 |
| I | 8 | 9 | זמן יצירה |
| J | 9 | 10 | תקף עד (8 שעות) |
| K | 10 | 11 | פעיל (boolean) |
| L | 11 | 12 | כמויות JSON |
| M | 12 | 13 | מאושרים JSON |
| N | 13 | 14 | בוחן אחראי |
| O | 14 | 15 | אוכלוסיית ברירת מחדל |

עמודות N ו-O חסרות בשורות ישנות — הקוד קורא אותן הגנתית (`data[i].length > 13`). **שמור על הדפוס הזה** בכל עמודה חדשה.

---

## 3. `תוצאות` — הרשומה הקבועה

30 עמודות. הנפוצות בקוד:

| עמודה | קריאה | שדה | | עמודה | קריאה | שדה |
|---|---|---|---|---|---|---|
| A | 0 | תאריך | | P | 15 | פירוט שגויות |
| B | 1 | ת.ז. | | Q | 16 | נשלח? |
| C | 2 | שם | | R | 17 | **פסול?** |
| D | 3 | טלפון | | S | 18 | קישור וואטסאפ |
| E | 4 | דרגה | | T | 19 | אוכלוסייה |
| F | 5 | ציון | | U | 20 | תוקן? |
| G | 6 | אחוז | | V | 21 | **שמע** |
| H | 7 | **עבר/נכשל** | | W | 22 | מאומת |
| I | 8 | זמן | | X | 23 | חשוד |
| J | 9 | בוחן | | Y | 24 | dqEventId |
| K | 10 | אתר | | Z–AB | 25–27 | תוקן ע"י / סיבה / תאריך |
| L | 11 | כיתה | | AC | 28 | מסלול שפות |
| M | 12 | שפה | | AD | 29 | מכשיר |
| N | 13 | קוד סשן | | | | |
| O | 14 | ניסיון | | | | |

**מוסכמות:**
- שורות **לא נמחקות**. תוצאה שבוטלה מסומנת `בוטל` בעמודה H ומדולגת בדו"חות.
- הגשה מאוחרת גוברת על שורת `פסול`/`טיימאאוט` קודמת.
- עמודות W ואילך חסרות בשורות ישנות — תמיד קריאה הגנתית לפי `length`.

---

## 4. שאר הגיליונות

| גיליון | תפקיד | הערה |
|---|---|---|
| `בוחנים` | משתמשי בוחן | סיסמה, תפקיד, טוקן, נעילה אחרי כשלונות, אתרים מנוהלים |
| `מורים` | משתמשי מורה | מבנה דומה |
| `אתרים` | בסיסים/אתרים | + כיתות |
| `כיתות` | כיתות לימוד | קוד כיתה, מורה, דרגה |
| `כיתות שנמחקו` | ארכיון | מחיקת כיתה משאירה תוצאות יתומות; הארכיון משחזר את שם המורה. **רק מחיקות עתידיות מתועדות** |
| `תלמידי כיתות` | שיוך תלמיד לכיתה | |
| `תוצאות תרגול` | תרגול עצמי | מבנה **שונה** מ`תוצאות` — פירוט שגויות הוא JSON, זמן הוא `M:SS` |
| `התקדמות תלמידים` | streak, שאלות שגויות, היסטוריה | לחזרה מרווחת |
| `הארכות זמן` | ביקורת הארכות באמצע מבחן | סיבה מתועדת חובה |
| `מבחנים` | מפת השאלות של כל מבחן (מזהה, סדר תשובות, אינדקס נכון) | נכתבת ב-`startExam`, נקראת בהגשה (דרך מטמון `qmap_` ואז סריקת עמודות A–B בלבד) |
| `ממתינים_ארכיון`, `מבחנים_ארכיון`, `תוצאות_ארכיון` | ארכוב לילי 01:00 (`archiveSheets`): 14 יום / 2 ימים / 30 יום | קוראי היסטוריה מאחדים חי+ארכיון דרך `readResultsSince`; **הגיליונות החיים נשארים קטנים** |
| `אבחון` | שורות SLOW/KILLED/CLIENT | מפסק כשהכתיבה עצמה איטית |
| `חיזוי סיכון` | פלט ה-batch הלילי | נבנה מחדש, לא מקור אמת |

---

## 5. מטמון ונתונים שאינם בגיליון

| מה | איפה | תוקף |
|---|---|---|
| בנק השאלות (טקסטים) | `assets/` של ה-Worker `session-gateway` (`q/<id>.json` לכל שאלה בכל השפות + `bank/<lang>.json` מלא), מיוצר מ-`deployment/generated/`; לא בגיט ולא ב-Pages | מוגש רק לפי אישור חתום (HMAC, `GATEWAY_KEY`): מבחן 4 שעות ל-30 מזהים, תרגול 2 שעות, בוחן 8 שעות; הלקוח מחזיק בזיכרון בלבד |
| מפתח התשובות + אינדקס מזהים | `answer_key.gs` + `QUESTION_INDEX` בתוך קובץ השרת | אין Drive, אין מטמון, אין חימום |
| מפת המבחן של נבחן (`qmap_<session>_<id>`) | CacheService | 3 שעות; נפילה: סריקת `מבחנים` A–B + שורה אחת |
| snapshot של `ממתינים` לסשן (`pendsnap_`) | CacheService | 4 שניות; כל כותב סטטוס מבטל דרך `setPendingStatus` |
| הגבלות קצב, אימות טוקן בוחן (60 שנ'), הארכות (30 שנ') | CacheService | לפי חלון |
| snapshot לסשן עבור ה-Worker | Cloudflare Cache API (`session-gateway`) | 2 שניות טרי, 60 שניות ישן; לוח הבוחן מבטל דרך `POST /v1/invalidate` אחרי כל החלטה |
| אישור בוחן לבנק (`ext_examiner_bank`) | localStorage של דף הבוחן / חיפוש התמונה | 8 שעות; מתחדש ב-`bankGrant` |
| דו"חות שהועלו | Cloudflare KV | קבוע |

(היסטורי: עד r30 הבנק נקרא מ-Drive למטמון CacheService עם חימום; המטמון היה ניתן להרעלה. הכול הוסר. ב-21/09/2026 בבוקר תוכנן בנק ציבורי ב-Pages; באותו ערב, לפי החלטת יוסי, הוחלף בבנק פרטי ב-Worker לפני שנפרס.)

---

## 6. הוספת עמודה — הנוהל

1. הוסף **בסוף בלבד**.
2. קרא הגנתית: `(row.length > N) ? row[N] : ''`.
3. עדכן את `SHEET_HEADERS` (לגיליונות חדשים) **וגם** את המסמך הזה.
4. זכור שהעמודה ריקה בכל השורות ההיסטוריות — אל תניח ערך.
5. אם היא צריכה לזרום לדו"חות — עדכן גם את מסלולי ההעתקה ל`תוצאות`.
