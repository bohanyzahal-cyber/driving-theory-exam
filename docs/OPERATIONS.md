# נהלי תפעול ופריסה

> קרא לפני **כל** פריסה. רוב התקלות ההיסטוריות במערכת הזו לא היו באגים בקוד אלא בפריסה שלא הושלמה.

---

## 0. הכלל שחייבים להפנים

**שלושה יעדי פריסה נפרדים. `git push` מעדכן רק אחד מהם.**

| יעד | מה נפרס | איך | מי יכול |
|---|---|---|---|
| GitHub Pages | קבצי HTML/JS/PWA | `git push` לענף `master` | אוטומטי |
| Google Apps Script | שלושה קבצים **מיוצרים** מ-`server/src/` (r31, DESIGN §13.3): `external_exam_apps_script.js` (מונוליט — כל הפעולות), `external_exam_apps_script.exam.js` (מבחנים — הפרויקט הקיים), `external_exam_apps_script.reports.js` (דו"חות/מורים/תרגול/ניהול/לילה — פרויקט עצמאי שני) | הדבקה ידנית בעורך + גרסה חדשה | רק בעל החשבון |
| Cloudflare Workers | `cloudflare-workers/*` (דו"חות, session-gateway) | `npx wrangler deploy` | בעל חשבון Cloudflare |

שינוי בקובץ ה-Apps Script שנדחף לגיט **אינו באוויר**. זה תפס אנשים שוב ושוב.

---

## 1. פריסת לקוח (GitHub Pages)

### מתי מותר לדחוף

| קובץ | מתי | למה |
|---|---|---|
| `examinee.html` | **רק כשאין נבחנים ממתינים או במבחן** | הדף מרענן את עצמו תוך ~2 דקות מהפריסה |
| `student.html` | עדיף בשעה שקטה | אותו מנגנון רענון שקט |
| `examiner.html` | כל זמן | מציג באנר עם שהות 60 שניות |
| `teacher.html` | כל זמן | אותו הדבר |

מנגנון הרענון: כל דף בודק את ה-ETag של עצמו כל 2 דקות ומרענן כשזוהה שינוי. ב-`examinee.html` הרענון חסום כשיש מבחן פעיל **או נבחן רשום** (המגן הורחב 28/07/2026); ב-`student.html` הבדיקה צרה יותר.

**תמיד לשאול את המשתמש אם יש בחינות לפני דחיפת דף שנוגע לנבחנים.**

### הנוהל

```bash
cd standalone_exam
git status
git add <files>
git commit
git push origin master
```

הענף הוא **`master`** (לא `main`), והרימוט הוא `driving-theory-exam` — לא מאגר השורש.

### אימות שהפריסה עלתה

אל תסתמך על "נראה תקין בדפדפן" — יש שכבת service worker ומטמון HTTP של 600 שניות שיכולים להטעות.

```bash
gh api repos/bohanyzahal-cyber/driving-theory-exam/pages/builds/latest --jq '.status'
```

ואז שליפה טרייה עם עוקף מטמון, מול סמן ייחודי מהשינוי:

```bash
curl -s -H 'Cache-Control: no-cache' "https://bohanyzahal-cyber.github.io/driving-theory-exam/examiner.html?cb=$(date +%s)" | grep -c "<סמן ייחודי>"
```

בחר סמן ב-ASCII (שם משתנה, לא טקסט עברי) — קונסולת Windows הופכת עברית ל-`?????`.

בנייה נמשכת בדרך כלל 30-90 שניות. לפעמים ה-API עדיין מדווח `building` בזמן שהתוכן כבר מוגש — **השליפה בפועל היא הקובעת**.

---

## 2. פריסת שרת (Google Apps Script)

### הנוהל

0. **הקבצים מיוצרים.** מקור השרת הוא `server/src/*.js` (סדר ב-`server/BUILD_ORDER.json`, שיוך לפריסה ב-`server/BUILD_TARGETS.json`). לעולם לא עורכים את `external_exam_apps_script*.js` ישירות. הבנייה מסירה הערות (acorn, `tools/vendor/`) ומאמתת שה-AST זהה למקור, ומזריקה את אינדקס השאלות כמחרוזת דחוסה (DESIGN §13.1):
   ```bash
   node tools/build.js
   ```
   בונה את הבנק הסטטי (`bank/`), את `deployment/question_index.json`, מרכיב את קובץ השרת (כולל `node --check`), ומעדכן `version.json` ושמות המטמון ב-service workers. `node tools/build_server.js --check` אומר אם הקובץ המחויב בגיט מעודכן.
1. **בדיקות:** `node tools/test.js` — כל החבילות חייבות להיות ירוקות.
2. פתח את פרויקט ה-Apps Script המקושר לגיליון.
3. החלף את **כל** תוכן הקובץ (הדבקה מלאה — כך זה נעשה תמיד כאן). **איזה קובץ:** בפרויקט הקיים (מבחנים) `external_exam_apps_script.exam.js` אחרי הפעלת הפיצול, או `external_exam_apps_script.js` (המונוליט) לפניה; בפרויקט הדו"חות `external_exam_apps_script.reports.js`. `health` מחזיר `deployment` — `exam` / `reports` / `all` — ואפשר לוודא שהודבק הקובץ הנכון. `answer_key.gs` הוא קובץ נפרד בעורך (בשני הפרויקטים — התרגול מנקד ממנו) ומודבק רק כשהמפתח משתנה. פעולה שנשלחה לפרויקט הלא-נכון עונה `wrong_deployment` (הדפים מנתבים לפי פעולה: `ExamTransport.REPORTS_ACTIONS`).
4. `Deploy` → `Manage deployments` → עיפרון → `Version: New version` → `Deploy`.

**קריטי:** בלי בחירת `New version` הקוד נשמר אבל הכתובת הציבורית ממשיכה להגיש את הגרסה הישנה. תקלה שקטה ומבלבלת. (טריגרים, לעומת זאת, רצים על הקוד החדש מיד עם השמירה.)

### תופעת הלוואי של ההדבקה המלאה

הדבקה מלאה פורסת **את כל מה שהצטבר בקובץ**, לא רק את השינוי האחרון. אם עברו שבועות מההדבקה הקודמת, בדוק מה נכנס:

```bash
git log --oneline -20 -- external_exam_apps_script.js
```

ואמור למשתמש מה עוד עולה איתו. אחרת שינוי התנהגות בלתי מוסבר ייזקף לשינוי האחרון.

### אימות שהשרת חי

```bash
curl -s -L --max-time 45 "<EXEC_URL>?action=getSessionInfo&sessionCode=ZZZZZZ&origin=examinee-app"
```

תשובה תקינה: `{"status":"error","message":"קוד סשן לא תקין"}` — כלומר ה-handler רץ.

- **בלי** `&origin=examinee-app` תקבל `{"status":"error","message":"Missing origin"}`. זו התנהגות **תקינה** של ה-allowlist, לא תקלה.
- זמן תגובה תקין: כ-2-9 שניות. עשרות שניות = רעב לחוטי ביצוע, ראה §5.

ה-`EXEC_URL` נמצא בתוך כל דף לקוח כ-`API_URL`.

### שתי כתובות API (מכוון מאז r31, 22/09/2026)

(פריסת "Worker" נפרדת של פרויקט המבחנים נוסתה 22/09 11:00–11:20 והוסרה — לא עזרה, כי העיכוב הוא בצעד המסירה של גוגל לכל הפרויקט; KNOWN_ISSUES #35. אם היא עדיין קיימת בניהול פריסות אפשר לארכב אותה.)


`API_URL` בדפים = פרויקט המבחנים (`…AKfycbzOI0zrDEngP…/exec`, הקיים); `REPORTS_API_URL` = פרויקט הדו"חות/מורים/תרגול (`…AKfycbw7FwTioHoEM…/exec`, עצמאי). `ExamTransport.REPORTS_ACTIONS` קובע מה הולך לאן; `health` בכל אחד מחזיר `deployment`. שינוי כתובת = עדכון הקבוע בחמשת הדפים (examiner, teacher, student, exam, find_image) + push; הבדיקות (`client_examiner`, `client_teacher_student`) מצמידות כתובת אחת לכל האתר.

### ⚠️ הכתובת השלישית (ישנה)

ששת הדפים (`examiner`, `examinee`, `teacher`, `student`, `admin`, `exam`) מצביעים על deployment אחד. **`report.html` מצביע על deployment אחר.** אם אתה מחליף כתובת API — בדוק את שניהם. לא אומת אם ההפרדה מכוונת או שריד היסטורי; **לברר לפני נגיעה**.

---

## 3. פריסת Cloudflare Workers

```bash
cd cloudflare-workers/session-gateway     # סקר הנבחנים + בנק השאלות הפרטי (assets/)
node ../../tools/build_bank.js            # מייצר assets/ מהדאמפים (לא בגיט) — חובה לפני כל deploy
npx wrangler deploy                       # מעלה את הקוד וגם את 1,700 קבצי הבנק
```
(ה-Worker של קישורי הדו"חות: `cd cloudflare-workers && npx wrangler deploy`.)

**אל תפרוס דרך דשבורד Cloudflare.** תועד שם כישלון כפול: הדבקת קוד Modules לעורך מסוג Service-Worker נכשלת בשקט, ו-Deploy מהעורך מחזיר 403. `wrangler` עובד.

Workers פעילים: `session-gateway` (סקר הנבחנים + הבנק, `wrangler.jsonc` עם `assets`), קישורי דו"חות (`steep-night-dd06`, עם KV), TTS עברית (`hebrew-tts`), פרוקסי תמונות.

**תקציב הבקשות (תוכנית חינמית):** 100,000 בקשות ביום לכל ה-Workers של החשבון יחד (`session-gateway`, `hebrew-tts`, פרוקסי התמונות, קישורי הדו"חות), מתאפס ב-00:00 UTC (03:00 ישראל בקיץ). מעבר לזה Cloudflare מחזיר שגיאה 1027 עד האיפוס — מבחנים שרצים ממשיכים (השאלות במכשיר), אבל מבחן חדש לא יכול להתחיל. עם הסקר הארוך (DESIGN §12) הצפי ~100 בקשות לנבחן (400 נבחנים ≈ 40–70 אלף). לקרוא את המונה: Cloudflare → Workers & Pages → `session-gateway` → Metrics (וגם `hebrew-tts`).

אימות `session-gateway`: `curl -s https://session-gateway.bohanyzahal.workers.dev/` מחזיר `bank` לא ריק; `…/v1/bank?grant=bogus` → 403; `…/bank/he.json` → 404 (הבנק אינו נגיש ישירות); r31: `…/v1/session/watch?sessionCode=ABCDEFGH` (בלי אישור) → 403; `curl -X POST "…/v1/invalidate?sessionCode=ABCDEFGH&idNumber=1&examineeToken=x"` → 403 (טוקן לא תואם).

אימות: `curl https://steep-night-dd06.bohanyzahal.workers.dev/` — אמור להחזיר `"links":"permanent"`.

Secrets שורדים פריסה. חיבור ה-KV נשמר כי הוא ב-`wrangler.toml`.

---

## 4. עדכון בנק השאלות

**מאז 21/09/2026 בערב בנק השאלות פרטי ומוגש מה-Worker:** הטקסטים (בלי התשובות הנכונות) יושבים ב-`cloudflare-workers/session-gateway/assets/` (לא בגיט; מיוצרים מ-`deployment/generated/questions_<lang>.json`, המקור, לא בגיט, 25MB) ומוגשים על ידי ה-Worker רק לפי אישור חתום: נבחן/תלמיד מקבלים בדיוק את השאלות שהונפקו להם (`startExam`/`startPractice` → `bank.grant`), בוחן מקבל אישור בוחן (`bankGrant`) לחיפוש ולטבלת "שאלות שגויות". השרת מחזיק רק `QUESTION_INDEX` (נושא לכל דרגה + זמינות לפי שפה, מוזרק לקובץ השרת בבנייה) ואת מפתח התשובות `deployment/answer_key.gs` (קובץ נפרד בעורך, לא בגיט). אין Drive, אין מטמון שאלות, אין חימום, ואין בנק ב-Pages. פרטים: `DESIGN_2026-09-21.md` §11.

הנוהל לשינוי שאלה/תרגום:

1. ערוך את ה-JSON של השפה ב-`deployment/generated/`. **התשובה הנכונה חייבת להישאר באותו אינדקס** — אחרת `answer_key.gs` צריך להתעדכן באותו קומיט (`deployment/generate_answer_key.js`).
2. `node tools/build.js && node tools/test.js` — `bank_invariants` מוודא שלכל שאלה בכל שפה יש מפתח בטווח, שאין תמונה חסרה, שהאינדקס תואם לבנק ושלכל מזהה יש קובץ `q/<id>.json`.
3. `cd cloudflare-workers/session-gateway && npx wrangler deploy` — מעלה את ה-assets החדשים. הלקוח מקבל תמיד את הגרסה שב-Worker (אין מטמון בדפדפן ל-`/v1/bank`).
4. אם השתנה אינדקס השאלות (`deployment/question_index.json` → `QUESTION_INDEX_PACKED` בקובץ השרת; שאלה נוספה/הוסרה/החליפה דרגה) — גם הדבקת קובץ השרת + גרסה חדשה (שני הפרויקטים אחרי הפיצול — התרגול שולף מאותו אינדקס). אם השתנה המפתח — גם הדבקת `answer_key.gs`. שינוי טקסט בלבד = Worker בלבד.
5. אימות: `curl -s https://session-gateway.bohanyzahal.workers.dev/` מציג `bank` = ה-build החדש (מ-`assets/manifest.json`); `health` מציג `indexIds`.

---

## 5. חירום

**כל המתגים, הסודות והמאפיינים במקום אחד:** `STATE_2026-09-21.md` §2 (ומה הוסר ב-r30). לפני שנוגעים במאפיין — לקרוא שם מה הוא עושה; `GATEWAY_URL` אינו מתג כיבוי.

### כל הדפים "לא מתקשרים"

בדוק **קודם** את זמן התגובה של ה-API (§2). עשרות שניות או 0 = רעב לחוטי ביצוע ב-Apps Script, לא באג בלקוח.

שתי תבניות מתועדות:
- **~13:00, כ-15 דקות, נפתר מעצמו** — פקיעת מטמון השאלות וגל תחילת-מבחנים על מטמון קר.
- **9:00-11:00** — סריקות כבדות בדשבורד הבוחן.

שתיהן טופלו (מאז 22/09/2026 אין מטמון שאלות ואין חימום; לוח הבוחן ממודד ל-3 קריאות). אם התסמין חוזר — גיליון הווטשדוג קודם (§5, בוקר בחינות).

### שאלות לא נטענות

אין יותר מטמון שאלות בשרת ואין בנק ב-Pages. שלושה מצבים:

- **`bank_not_configured` בהתחלת מבחן** — חסר `GATEWAY_KEY` או `GATEWAY_URL` ב-Script Properties (`health&deep=1` מציג `gateway:{url,key}`). להשלים את המאפיינים; אין צורך בפריסה.
- **"לא הצלחנו לטעון את השאלות"** (הדף מנסה שוב לבד) — ה-Worker לא עונה או מחזיר 403/503: `curl -s https://session-gateway.bohanyzahal.workers.dev/` חייב להחזיר `bank` לא ריק. `bank` ריק = ה-assets לא הועלו (`node tools/build_bank.js` ואז `npx wrangler deploy`). 403 על אישור אמיתי = `GATEWAY_KEY` שונה בין ה-Worker (`wrangler secret`) לשרת (Property) — לקבוע את אותו ערך בשניהם.
- **"שאלה לא זמינה"** על שאלה בודדת — מזהה שקיים באינדקס אבל לא ב-assets: הבנייה וה-Worker לא מאותו build. `node tools/build.js` ואז `npx wrangler deploy` וגם הדבקת השרת.

התחלת מבחן נכשלת עם `bank_unavailable` רק כשהאינדקס בשרת ריק — כלומר הודבק קובץ שרת בלי הזרקת האינדקס (לבנות מחדש עם `node tools/build.js`).

### טלפון שלא מגיע ל-Worker (מאז r33, 24/09/2026 — KNOWN_ISSUES #38)

**מה אמור לקרות לבד:**
- בבדיקת החיבור במסך הקוד, או אחרי 3 כשלים, הטלפון עובר למסלול של גוגל.
- הנבחן רואה שורה ירוקה "📡 … חיבור חלופי דרך Google … אין צורך לרענן".
- בלוח מופיע ליד השם תג כחול "📡 גיבוי גוגל". השורה עלולה להופיע באיחור של עד כ-45 שנ׳.
- אישור מגיע לטלפון תוך כ-12 שנ׳.
- השאלות מגיעות דרך השרת (`bankRelay`).

**מה עושה הבוחן:**
- **טלפון עם 📡 — רגיל.** מאשרים כרגיל. לא לאפס ולא לאשר שוב.
- **טלפון תקוע בלי 📡, עם "בעיית תקשורת"** — לפתוח לו את הקישור של הסשן ועוד `&gw=google` בסוף. זה שולח אותו ישר לגוגל.
- **גם דרך גוגל לא עובד** (באנר אדום על רקע השורה הירוקה) — לטלפון אין אינטרנט: להזיז למקום עם קליטה, נקודה חמה, או נייר.
- **כדי לדעת למה**, בטאב חדש בטלפון פותחים `https://session-gateway.bohanyzahal.workers.dev/cdn-cgi/trace` ומצלמים. דף טקסט = הטלפון מגיע ל-Cloudflare. `error code: 1010` או דף "Just a moment" = Cloudflare חוסם.
- **(r34) ואז גם את הכתובת שלנו:** `https://api.teoria-digital-vitaly.com/`. זה אותו Worker. אם היא עונה (`"status":"ok"`) והראשונה נתקעת, החסימה היא על השם `workers.dev`. כדאי לשאול את הנבחן אם יש לו מסנן, למשל "גלישה בטוחה" של הספק או מסנן כשר.

**אחרי היום:**
- בגיליון `אבחון` יש שורות CLIENT עם `"e":"gw"`. בכל אחת: הדפדפן, סוג השגיאה ו-`trace` של טלפון שעבר לגוגל. מאז r34 יש שם גם `alt=`: האם הכתובת שלנו ענתה לאותו טלפון.
- בלוג של כל הגשה יש `gw_alt`, כלומר התשובה גם מטלפונים שלא עברו לגוגל.
- **24/09:** 19 מתוך 21 הטלפונים שעברו לגוגל קיבלו `trace=timeout`. זו שתיקה מוחלטת, לא חסימה עם תשובה (KNOWN_ISSUES #38).
- **ההחלפה לכתובת שלנו** נעשית רק לפי הנתונים. היא Script Property `GATEWAY_URL` בשני הפרויקטים, בלי פריסת דפים (TODO 0א.13).
- הבדיקה מפרויקט Apps Script נפרד (17:11, 23/09) הראתה שגוגל כן מגיע ל-Worker. אם זה ישתנה, `bankRelay` יחזיר `relay_failed`. בשרת יש `testGatewayReachability()` להרצה מהעורך.

### ה-Worker (session-gateway) לא זמין

**מאז 21/09 בערב ה-Worker הוא תלות קשה של התחלת מבחן** (הטקסטים מגיעים רק ממנו) — כמו ש-Drive היה עד r30. עד r33 הסקר עבר רק דרכו (יוסי, 21/09: "Cloudflare הוא הדרך, בלי 'מה עושים אם'"); **מ-r33 (24/09, אחרי 23/09) טלפון שלא מגיע ל-Worker עובר לגוגל לבד** — הסקר ישיר בקצב איטי, והטקסטים דרך `bankRelay`, שהשרת מביא מה-Worker (ראו הסעיף הקודם). לכן אם Cloudflare כולו נופל, הצי עובר לגוגל בקצב מוגבל, ומבחנים שכבר רצים ממשיכים. התחלת מבחן חדש עדיין דורשת ש**גוגל** יגיע ל-Worker. **למחוק את `GATEWAY_URL` = אין טקסטים = אין מבחנים** — זה כבר לא מתג כיבוי אלא חלק מחזרה מלאה לאחור (`DEPLOY_2026-09-22.md` §8: גרסת Apps Script קודמת + לקוח r24 ב-Pages, שמקבל שאלות מהשרת). סטטוס Cloudflare: https://www.cloudflarestatus.com/.

### לקוח שבור — החזרה לאחור

```bash
cd standalone_exam
git revert <commit> --no-edit
git push origin master
```

**החזרת שרת לאחור דורשת הדבקה ידנית של גרסה קודמת.** אין דרך מהירה. לכן שרת נפרס ראשון בשינויים אדיטיביים — שרת חדש מול לקוח ישן הוא no-op בטוח.

### תוצאה שנעלמה

תוצאות נשמרות מקומית עד לאישור השרת, ונשלחות מחדש בטעינת הדף. לפני שמכריזים על אובדן — בדוק `localStorage` במכשיר, וחפש שורות `פסול`/`טיימאאוט` בגיליון שהוחלפו על-ידי הגשה מאוחרת.

---

### גיליון אבחון: שורות SLOW שלא הופיעו

מאז r22 (19/09/2026) שורת SLOW נכתבת לגיליון `אבחון` רק כל עוד הכתיבה עצמה בריאה: כתיבה שנכשלה או שלקחה יותר מ־2 שניות פותחת "מפסק" ל־5 דקות, ובזמן הזה השורות נחנות ב־ScriptProperties במקום להוסיף כתיבות למסמך שכבר תקוע (ב־17/09 כתיבת האבחון עצמה נתקעה 93 שניות; ב־16/09 לפחות שבע שורות של הרצות ארוכות אבדו). השורות החנויות נכתבות לגיליון בבדיקה השעתית (`ensureQuestionCachesWarm`), או מיד בהרצת `flushDiagnostics` מעורך הסקריפט. אם בבוקר בחינות הגיליון נראה ריק בזמן שהלוחות איטיים, להריץ `flushDiagnostics` לפני שמסיקים "לא היה כלום". הסימונים החדשים בעמודת ההערות: `ss:open` (פתיחת המסמך), `auth:token` (קריאת גיליון הבוחנים לפני המטפל), `tail:small/n` / `tail:1000/n` / `tail:full/n` (איזו קריאה בוצעה בפועל).

### בוקר בחינות — נוהל תפעולי (מהבדיקה של 18/09/2026)

**מ-22/09/2026 (החזרה בבוקר, אחרי r31):** (א) **המחשב של הבוחן לא נרדם ולא נועל מסך** כל הבחינה, והלוח בכרטיסייה קדמית — לוח שלא מתעדכן = מחשב שישן (נמדד: 24 דקות בלי אף בקשה מהלוח; התוצאה "הגיעה" ברגע שהמחשב התעורר). (ב) **המחשב ברשת של האתר**, לא הוטספוט של טלפון — הוטספוט עם קליטה חלשה מפיל את הלוח ואת הטלפונים יחד. (ג) אחרי כל פריסה, טלפונים שנשארו עם הדף פתוח מיום קודם: לסגור ולפתוח את הכתובת מחדש פעם אחת.


1. **המקום הראשון להסתכל בו כשמשהו איטי הוא גיליון הווטשדוג** (`tools/exam_watchdog.gs`, סקריפט נפרד שמודד כל דקה 07:00–13:00): עמודת `verdict` אומרת `our-document` (המסמך שלנו נתקע), `dispatch`/`google/account` (גוגל), או `ok` בזמן שהבוחנים מתלוננים (הבעיה בלקוח/ברשת). התקנה חד-פעמית מתוארת בראש הקובץ.
2. **אישורים מדורגים** — לאשר כ-5 נבחנים כל 2 דקות ולא כיתה שלמה בבת אחת. גל של 43 רישומי מבחן בדקה (17/09, 09:21) הוא הכתיבה הכבדה ביותר על המסמך, ובדיוק בדקות האלה נתקעו הקריאות.
3. **מורים לא פותחים "פרטי כיתה" בין 09:00 ל-11:00 בימי בחינות** — כל פתיחה היא קריאה מלאה של 107 אלף שורות מאותו מסמך. זה מגבר, לא הסיבה, אבל מגבר שאפשר לכבות בטלפון אחד.
3א. **(24/09/2026) לוח המפקד ודו"ח מפקד מרכז חסומים בשרת בזמן בחינות.** ב-24/09 שלוש פתיחות של לוח המפקד ב-10:20–10:22 (60, 55, 42 שנ׳), ובדקה שאחריהן פתיחת המסמך בפרויקט המבחנים נתקעה 243 שנ׳. עכשיו, כל עוד יש נבחן ממתין / מאושר / במבחן בסשן פתוח (K=TRUE ו-J לא עבר) שהתחיל מבחן — או נרשם, אם עוד לא התחיל — בשעתיים האחרונות, השרת עונה במקום הדו"ח "יש עכשיו בחינות פעילות — N נבחנים ב-M סשנים…" (קוד `exam_hours`), אחרי קריאה קלה אחת (`סשנים` + זנב `ממתינים`, כמו רענון אחד של לוח בוחן). **לא נחסם:** כניסה לסשן פעיל ("📂 הצג כל הסשנים הפעילים"), הלוח שלו, תיקון ציון / פסילה / שורה ידנית — אלה פעולות של פרויקט המבחנים. תקלה בבדיקה עצמה לא חוסמת: הדו"ח רץ. הכלל: `liveExamActivity` ב-`server/src/86_commander.js`; נכנס לתוקף עם ההדבקה הבאה של `external_exam_apps_script.reports.js` בפרויקט הדו"חות.
4. **באנר "השרת עמוס / משיב בשגיאה" בלוח הבוחן = לא לרענן ולא להתחבר מחדש.** הדף ממשיך לבד (מאז r31 הלוח מחכה ל-Worker שיגיד "משהו השתנה" וקורא את גוגל רק אז; אם ה-Worker לא עונה הוא חוזר לקריאה כל 5 שניות; אף פעם לא מאט את עצמו). רענונים והתחברויות בו-זמנית מכל הארץ הם שיצרו ב-16/09 את הפרצים של 200+ בקשות בדקה.
5. **גיליון אבחון ריק בזמן איטיות ≠ "לא היה כלום"** — להריץ `flushDiagnostics` מהעורך (ראו למעלה).
5א. **(r33) באתר גדול — גלים של 15–20 נבחנים**, לא 60 בבת אחת. תג 📡 בלוח = טלפון במסלול של גוגל. זה תקין, מאשרים כרגיל (ראו "טלפון שלא מגיע ל-Worker"). **פסילה אוטומטית** מראה עכשיו גם את הסיבה בבאנר, למשל "(מסך מפוצל)" או "(יציאה מהמסך מעל 10 שנ׳)". באייפון "מסך מפוצל" כבר לא אמור לקרות; אם קרה, לפני ביטול הפסילה — aA ← 100% בספארי.
5ב. **(r34) פסילה עצמית מחכה לבוחן, גם באנדרואיד.**
   - עד 24/09 פסילה מאנדרואיד לא הגיעה לשרת, והטלפון חזר למבחן לבד אחרי כמה שניות (KNOWN_ISSUES #40).
   - עכשיו הטלפון שולח את הפסילה עד שהשרת מאשר, והלוח מציג אותה עם הסיבה.
   - הטלפון נשאר על "ממתין להחלטת הבוחן" עד "בטל פסילה" או "אשר פסילה".
   - אם מבטלים מיד (תוך שניות), הטלפון עלול לחכות עד 45 שנ׳ לפני שהמבחן חוזר. **לא ללחוץ שוב.**
6. אחרי הבוקר: לקרוא את עמודת ההערות באבחון — `ss:open`, `auth:token`, `tail:full` אומרים איפה עברו השניות שלפני המטפל.

## 6. סדר פריסה בשינוי מרובה-צדדים

1. **שרת ראשון** — אם השינוי אדיטיבי (שדות חדשים בתשובה, פרמטרים אופציונליים). לקוח ישן מתעלם.
2. **לקוח ראשון** — אם השרת נעשה מחמיר יותר (דרישת פרמטר חדש). אחרת הלקוח הישן נשבר ברגע הפריסה.

טעות בסדר כאן השביתה התחברות בעבר. אם לא ברור לאיזה סוג השינוי שייך — הנח שהוא מהסוג השני.

---

## 7. בדיקה בטוחה בפרודקשן

השתמש באתר **`בדיקת נתונים`** — הוא מסונן מסטטיסטיקות הדשבורד. גם `דימונה דוגית 35`. מוגדרים ב-`TEST_SITES`.

בנוסף, תוצאות שבהן ת.ז. או שם הנבחן תואמים בוחן מסוננות אוטומטית — בוחן שבודק את עצמו לא מזהם נתונים.

---

## 8. רשימת תיוג לפני סיום עבודה

- [ ] `node tools/build.js && node tools/test.js` ירוקים, ו-`node tools/build_server.js --check` אומר שהקובץ מעודכן
- [ ] בלוקי ה-`<script>` בקבצי HTML שנגעת בהם מתקמפלים
- [ ] נשאלה שאלה על בחינות פעילות לפני דחיפת דף נבחנים
- [ ] הפריסה אומתה בשליפה טרייה, לא בהצצה בדפדפן
- [ ] אם נדרשת הדבקת Apps Script — נאמר למשתמש במפורש
- [ ] אם נפרס שרת — נאמר מה עוד עלה איתו

---

## 9. הפרדת נתוני התרגול והמורים לגיליון נפרד (r24, 19/09/2026)

**למה:** המסמך הוא יחידת התחרות. "פרטי כיתה" של מורה = קריאה מלאה של 107 אלף שורות מאותו מסמך שסקרי הבחינה קוראים וכותבים אליו, וערב תרגול לבדו הגיע לסף הדחייה של גוגל (16/09 19:46). שבעת גיליונות התרגול/המורים (`תוצאות תרגול`, `כיתות`, `תלמידי כיתות`, `מורים`, `התקדמות תלמידים`, `כיתות שנמחקו`, `חיזוי סיכון`) עוברים לגיליון Google נפרד. הקוד מנתב לפי שם הגיליון דרך ה-Script Property `PRACTICE_SPREADSHEET_ID`; כל עוד הוא לא מוגדר, הכול ממשיך לעבוד מהגיליון הישן.

**הנוהל, בסדר הזה:**

1. להדביק את קובץ השרת בעורך ולשמור.
2. להריץ מהעורך `practiceMigrationPreflight` (קריאה בלבד). בפעם הראשונה גוגל תבקש הרשאה חדשה — גישה מלאה לגיליונות (בגלל `openById`). לאשר. הדוח חייב להכיל "no formulas reference sheets across the practice/exam boundary". אם מופיעות נוסחאות שחוצות את הגבול — לעצור ולטפל בהן קודם.
3. "פריסה ← ניהול פריסות ← עריכה ← גרסה חדשה". מרגע זה הגרסה החיה יודעת לקרוא את ה-Property, אבל עדיין קוראת מהגיליון הישן.
4. **בשעה שקטה (ערב, שבת, לא ביום בחינות):** להריץ `migratePracticeSpreadsheet`. ההעתקה נעשית בנתחים של 4,000 שורות עם תקציב של 4.5 דקות לריצה (העתקת גיליון בפקודה אחת נכשלה על 109 אלף השורות: "Unable to load document"). ריצה שנגמר לה הזמן מסתיימת ב-`NOT cut over yet — run again`: להריץ שוב, היא ממשיכה מהשורה שבה עצרה. מהריצה הראשונה ועד ה-`CUT OVER` כתיבות תרגול נענות ב"מערכת התרגול בתחזוקה — נסה שוב בעוד דקה" (קריאות ממשיכות). היומן של הריצה האחרונה מסתיים ב-`CUT OVER` ובכתובת הגיליון החדש.
5. מיד אחר כך: `verifyPracticeMigration` — כל שורה `ok` ובסוף `ALL OK`.
6. אחרי יום של עבודה רגילה: `retirePracticeSheetsFromExamSpreadsheet` משנה את שמות העותקים הישנים ל-`_migrated_…`. שבוע אחר כך למחוק אותם ידנית מהגיליון הישן — **רק אז** מסמך הבחינות באמת מתכווץ.
7. חזרה לאחור בכל שלב: `rollbackPracticeSpreadsheet` מחזיר את הניתוב לגיליון הישן ומשחזר שמות. שורות שנכתבו לגיליון החדש אחרי המעבר לא מועתקות חזרה.

**מה משתנה למורים ולתלמידים:** כלום. אותה כתובת, אותם דפים. הגיליון החדש נמצא ב-Drive של אותו חשבון; אם מישהו קורא את `תוצאות תרגול` ישירות בגיליון, לתת לו את הכתובת החדשה מהיומן.

---

## 10. חוק הפריסה (מאז 21/09/2026)

**אין פריסה — לא שרת, לא Pages, לא Worker, לא טריגר — ב-48 השעות שלפני יום בחינות, ולא בבוקר בחינות.** הראיות: 26/08, 14/09, 15/09, 16/09 (`docs_private/reviews/2026-09-21/ (מחוץ לריפו הציבורי)/B_forensic_timeline.md` §4). דחיפה ל-Pages מחתימה מחדש את כל הקבצים; הדבקה בעורך מריצה טריגרים על הקוד החדש מיד.

לפני כל פריסה, בסדר הזה:

1. `node tools/build.js && node tools/test.js` ירוקים.
2. `git log --oneline -20` — לדעת מה עוד עולה.
3. אין סשן פתוח (`listAllSessions` בלוח המפקד, או `סשנים` עמודה K).
4. סדר: Worker (אדיטיבי) → Script Properties → שרת (+ גרסה חדשה) → Pages → טריגרים. אחרי כל צעד אימות בשליפה טרייה (§1, §2, §3). כשהשינוי הוא בלקוח בלבד — Pages בלבד; בטקסט שאלה בלבד — Worker בלבד (§4).
5. מבחן מלא באתר `בדיקת נתונים` (הרשמה → אישור → התחלה → החלפת שפה → הגשה → `מאומת` בלוח).
6. רישום ב-`KNOWN_ISSUES`/זיכרון: מה עלה, מתי, מה אומת.

**חריג מתועד:** מהדורת r30 נפרסה ב-21/09/2026 בערב, לפני בחינות 22/09, בהחלטה מפורשת של הבעלים (הודעה 19: "פורסים היום, הסיכון ידוע לי"), עם חזרה מלאה באותו ערב ותוכנית חזרה לאחור מוכנה (`DEPLOY_2026-09-22.md`). החוק נשאר החוק לפריסות הבאות.

### בוקר בחינות — בדיקת 07:00

1. גיליון הווטשדוג: `watchdogMorningCheck` כתב שורה (verdict `ok`) — אם לא, לקרוא את ה-verdict לפני שנוגעים במשהו.
2. `health&deep=1` מחזיר את ה-build הצפוי, `sheetMs` < 1,000 ו-`gateway:{url:true,key:true}`.
3. `curl -s https://session-gateway.bohanyzahal.workers.dev/` מחזיר `bank` לא ריק; לוח הבוחן נפתח ומציג את הסשן.
4. לא לפרוס כלום. לא להריץ פונקציות מהעורך (חוץ מ-`flushDiagnostics` בסוף היום).

## 11. אתר שעבר למערכת החדשה — `MOVED_SITES` (מ-r35, KNOWN_ISSUES #44)

**מה זה:** כשאתר עובר למערכת החדשה, המערכת הישנה מפסיקה לפתוח לו סשנים. הבוחן שמנסה לפתוח סשן לאתר הזה (כאתר מארח או כאתר אורח) מקבל במסך פתיחת הבחינה:

> האתר "<שם>" עבר למערכת החדשה — יש להשתמש בקישור החדש: <הקישור>

(קוד `site_moved`). אין הפניה ואין מסלול עוקף.

**מה לא משתנה:**
- סשן שכבר פתוח לאתר ממשיך עד הסוף כרגיל: הרשמה, מבחן, תוצאה, לוח, דו"ח.
- כל אתר שלא ברשימה עובד כרגיל.
- כשהמאפיין חסר או ריק, אין שום שינוי.

**איך מוסיפים אתר** (בלי הדבקה ובלי גרסה חדשה — מאפיין בלבד; נכנס לתוקף מהבקשה הבאה):
1. בעורך Apps Script של **פרויקט המבחנים**: הגדרות הפרויקט ← מאפייני סקריפט (Script Properties). `createSession` רץ רק שם; בפרויקט הדו"חות אין צורך.
2. `MOVED_SITES` = **מערך JSON** של שמות אתרים, בדיוק כמו בגיליון `אתרים` / ברשימת האתרים במסך פתיחת הבחינה. רווחים בקצוות ורווח כפול לא משנים. למשל:
   ```
   ["בסיס 6","בח\"א 6"]
   ```
   גרשיים בתוך שם נכתבים `\"`. להוסיף אתר = להוסיף לרשימה. **לא למחוק** אתרים שכבר עברו.
3. (אופציונלי) `MOVED_SITES_URL` = הכתובת של המערכת החדשה. היא מופיעה בהודעה לבוחן ובשדה `url` של התשובה.
4. **לבדוק מיד:**
   ```
   curl.exe -s -L "https://script.google.com/macros/s/AKfycbzOI0zrDEngP-GvlRblhOk8tQsYBvWZ2gGliIQHTpS67WrDZl4la8NPpwtJr_Vjsh3Gzg/exec?action=health&origin=examiner-app"
   ```
   `"movedSites":<מספר האתרים ברשימה>`. אם יוצא `"movedSites":"invalid"`, הערך אינו מערך JSON של מחרוזות. **כל עוד הוא לא תקין, אף סשן חדש לא נפתח** (`moved_sites_invalid`) — בכוונה, כדי שטעות הקלדה לא תתפרש בשקט כ"אף אתר לא עבר". לתקן מיד.
   `"movedSites":"error"` (מ-r35.1): גוגל לא הצליח לקרוא את המאפיין (תקלה רגעית או מכסה). גם אז סשן חדש נדחה (`moved_sites_invalid`, עם "נסה שוב בעוד דקה"). לבדוק שוב אחרי דקה.
5. לשנות את המאפיין **רק מחוץ לשעות מבחן**. `node tools/morning_check.js` מסמן `MOVED_SITES-INVALID` / `MOVED_SITES-ERROR` בבדיקת הבוקר.

**להחזיר אתר למערכת הישנה:** להוריד אותו מהמערך (או למחוק את המאפיין כשאין אף אתר). הסשן הבא ייפתח כרגיל.

## 12. התרגול עבר למערכת החדשה — `PRACTICE_MOVED` (מ-r35.1, KNOWN_ISSUES #45)

**מה זה:** התרגול עובר למערכת החדשה בבת אחת, לכל הכיתות (תוכנית העבודה, שלב P). מהרגע שהמאפיין מוגדר, כל פעולה של התרגול בפרויקט **הדו"חות** עונה:

> התרגול עבר למערכת החדשה — יש להשתמש בקישור החדש: <הקישור>

(קוד `practice_moved`). הפעולות: דף התלמיד (`startPractice`, `submitPracticeResult`, `studentJoinClass`, `loadStudentProgress`, `saveStudentProgress`), דף המורה (כל פעולות `teacher*`, כולל הכניסה) ו-`admin.html` (`adminDashboard`). אין הפניה ואין מסלול עוקף, ושום דבר לא נכתב לגיליון.
- דף התלמיד מציג את ההודעה בחלון (`alert`) כשמתחילים תרגול. דף המורה מציג אותה במסך הכניסה. הודעה ייעודית בדפים עצמם מגיעה עם דחיפת Pages.

**מה לא משתנה:**
- המבחנים. פרויקט המבחנים לא קורא את המאפיין בכלל.
- דוחות הבחינות של פרויקט הדו"חות: `commanderDashboard`, `centerManagerReport`, `siteCombinedReport`, `examinerForecast`.
- כשהמאפיין חסר, ריק או `false`, אין שום שינוי.

**איך מפעילים** (בלי הדבקה ובלי גרסה חדשה — מאפיין בלבד; נכנס לתוקף מהבקשה הבאה; **רק ביום בלי תרגול מתוכנן**):
1. בעורך Apps Script של **פרויקט הדו"חות** (הפרויקט העצמאי): הגדרות הפרויקט ← מאפייני סקריפט. בפרויקט המבחנים אין צורך.
2. `PRACTICE_MOVED` = `true`. מותר גם `{"moved":true}`. כל ערך אחר (למשל `yes`, `1`, `"true"` עם גרשיים) הוא טעות.
3. (אופציונלי) `PRACTICE_MOVED_URL` = הכתובת של התרגול במערכת החדשה. היא מופיעה בהודעה ובשדה `url` של התשובה.
4. **לבדוק מיד:**
   ```
   curl.exe -s -L "https://script.google.com/macros/s/AKfycbw7FwTioHoEMvl6Plk-IlHii1rb3FSs9CXan-8lCVP5K7FTuz594rsEOc2y6LDVS-DXcQ/exec?action=health&origin=examiner-app"
   ```
   `"practiceMoved":true`. אם יוצא `"practiceMoved":"invalid"`, הערך לא תקין, ו**כל התרגול נדחה** (`practice_moved_invalid`) — בכוונה, כמו ב-`MOVED_SITES`: טעות הקלדה לא תתפרש בשקט כ"לא עבר". לתקן מיד. `"error"` = גוגל לא הצליח לקרוא את המאפיין; לבדוק שוב אחרי דקה.
5. `node tools/morning_check.js` מסמן `PRACTICE_MOVED-INVALID` / `PRACTICE_MOVED-ERROR`.

**לבטל:** `PRACTICE_MOVED` = `false`, או למחוק את המאפיין. התרגול הבא עובד כרגיל. לפי התוכנית זה לא אמור לקרות אחרי שלב P.
