# עבודה פתוחה

עודכן: 21/09/2026 בערב (מהדורת r30 עם §11 — ראה `DESIGN_2026-09-21.md`, `DEPLOY_2026-09-22.md`)

סדר העדיפויות הוא **הצעה** — הבעלים קובע. פריטים המסומנים `לאמת` מבוססים על היסטוריית הפרויקט ולא נבדקו מול המערכת החיה לאחרונה; התחל מאימות, לא מתיקון.

---

## 0. פריסת r30 (21/09/2026 בערב) ומיד אחריה

| # | נושא | איך |
|---|---|---|
| 0.1 | לאמת שהפריסה שלמה | `DEPLOY_2026-09-22.md` צעדים 2–7, כולל החזרה המלאה (15 שורות) באתר `בדיקת נתונים` |
| 0.2 | 22/09 07:00: בדיקת הבוקר (`DEPLOY_2026-09-22.md` §9); 23/09: לקרוא `אבחון` + `log` של הווטשדוג + מונה הבקשות של ה-Worker | שורות SLOW צריכות להיעלם כמעט לגמרי; verdict `ok` כל 2 דק'; מונה מתחת ל-100k |
| ~~0.3~~ | ~~מחיקת לשוניות `_migrated_`~~ | ✅ **22/09/2026 06:45:** `verifyPracticeMigration` (בפרויקט הדו"חות) — ALL OK, שבע הלשוניות במסמך התרגול; העותק הישן 109,492 ≤ 109,836 החדש; 7 הלשוניות `_migrated_` נמחקו. נשאר: מחיקת `server/src/98_migration_practice.js` (7.2) עם ההדבקה הבאה של קובץ הדו"חות |
| 0.4 | להסיר את ה-no-op `markExamStarted` ואת תשובת `client_outdated` של `getExamQuestions`/`registerExamQuestions` | אחרי שבוע בלי לקוחות ישנים (אין קריאות כאלה ב-`אבחון`/Executions) |
| 0.5 | `report.html` מצביע על deployment אחר של Apps Script | **נחקר 21/09 ערב, ממתין להחלטת יוסי (שואל מישהו):** הדף נוצר 24/02/2026 ("דו"ח בוחן"), נשכח כשכתובת השרת הוחלפה ב-04/03 (e89a729), שום דף לא מקשר אליו מאז עיצוב הדו"ח של יולי, ועדיין עולה באתר (200). הפריסה הישנה עונה בלי `build` (קוד מלפני חתימת הגרסאות), מדליפה פרטים פנימיים בהודעת שגיאה, ומחוברת לגיליונות החיים. המלצה: למחוק את הדף + לארכב את הפריסה מפברואר ב-Apps Script (פריסה ← ניהול פריסות ← ארכיון) |

---

## 0א. "הכל מהיר, בלי טיימרים" (החלטת יוסי 21/09 ערב, אחרי החזרה)

| # | נושא | מצב |
|---|---|---|
| 0א.1 | **הקטנת הקובץ המודבק** (565 KB; פעולה ריקה 2.7 שנ׳ = 1.4 גוגל + 1.2 שלנו): הסרת הערות בבנייה + QUESTION_INDEX כטקסט שנפתח בעצלות | ✅ **מומש בלילה 21–22/09 (r31, DESIGN §13.1)** — ממתין להדבקה של יוסי (`DEPLOY_2026-09-22.md` §11.1) |
| 0א.2 | **לוח בוחן בלי טיימר**: Worker `/v1/session/watch` (אישור בוחן, fp לסשן, החזקה) → `examinerDashboard` רק כשיש שינוי + רענון ביטחון | ✅ **מומש ופרוס בלילה 21–22/09 (r31, DESIGN §13.2)** — Worker + Pages |
| 0א.3 | **פיצול השרת** לשניים (מבחנים / דו"חות+מורים+תרגול+ניהול) — הקובץ החם קטן פי 3 | ✅ **מומש (r31, DESIGN §13.3)**: שלושה קבצים מיוצרים + ניתוב בדפים; **ההפעלה של יוסי** (פרויקט עצמאי שני, `EXAM_SPREADSHEET_ID`, טריגרים) לפי `DEPLOY_2026-09-22.md` §11.2 |
| 0א.4 | מעבר על כל קצב קבוע שנשאר (נבחן בלי החזקה, מורה, תלמיד, בדיקת גרסה): "למה לא 0" | ✅ **נעשה (DESIGN §13.4 — טבלת הדין לכל קצב)**: הסקר מחזיק 250 ms אחרי כל תשובה עם `fp`; המכשיר דוחף אחרי הגשה/"סיימתי"; `markPendingCompleted` מבטל snapshot; נשארים בכוונה: backoff על כשל, FRESH_MS 2 שנ׳, בדיקת גרסה 2 דק׳ |
| 0א.5 | להשלים חזרה (DEPLOY §7 שורות 10א–15), §7א (verify + מחיקת _migrated_ + run_once.gs), בדיקת 07:00 22/09 | ✅ 22/09 בוקר: §7א הושלם; חזרה חוזרת 06:57–07:00 עם לוג חי — נקייה (KNOWN_ISSUES #27ג); בדיקת 07:00 עוברת ל-23/09 (יום בחינות): ווטשדוג, health בשני הפרויקטים, Worker |
| 0א.6 | CPU להחזקת watch 6–11 ms (גבול התוכנית החינמית 10): לשקול HOLD_TICK_MS 2 שנ׳ או דילוג על קריאת המטמון כשהזיכרון טרי | פתוח, לא דחוף |

## 1. עדיפות גבוהה — נכונות

| # | נושא | פירוט |
|---|---|---|
| 1.5 | **`registerExaminee` לא בודק שהסשן קיים ופעיל** — נמצא 22/09 בבוקר: בדיקה עם קוד סשן מומצא (ZZZZZZZZ) יצרה שורת `waiting` אמיתית ב-`ממתינים` (בוטלה מיד). מגבלת הקצב (30/דקה) מגבילה הצפה, אבל שורות זבל כאלה גם דוחות את הארכוב הלילי ב-3 שעות | לבדוק `sessionRowByCode` + תוקף לפני הכתיבה (כמו `getSessionInfo`); בדיקה ב-api_reliability |
| ~~1.1~~ | ~~שומר `ci` בצד השרת~~ | ✅ **נסגר 22/09/2026** — הלקוח לא מנקד יותר; השרת מסרב לרשום/לנקד מפה קצרה או ריקה (S1) ומתייחס ל-`selected` לא-תקין כלא-נענה (S2) |
| ~~1.2~~ | ~~**`handleSubmitPracticeResult` לא מאמת קוד כיתה**~~ | ✅ 21/09 ערב: קוד כיתה נשמר רק אם הכיתה קיימת ב-`כיתות` והתלמיד רשום ב-`תלמידי כיתות` (2 קריאות עמודות לכל היותר); אחרת השורה נשמרת בלי כיתה ועונה `classUnknown:true` — התלמיד לא נפגע, הדו"חות נקיים |
| ~~1.3~~ | ~~מגן רענון ל-`student.html`~~ | ✅ נסגר (`49a9688` 16/09 + `version.json` 22/09) |
| 1.4 | **יישור סדר התשובות בתרגומים** (en/fr/es/ar בסדר משלהן) | הניקוד נכון; זו איכות נתונים: התאמת 3 המסיחים לכל שאלה לסדר העברי דורשת התאמת תרגום (רק הנכונה ידועה מהמפתח). החלטה 5 ב-`DESIGN_2026-09-21.md`: להשאיר עד שיוסי מחליט |

---

## 2. עדיפות בינונית — ביצועים ועמידות

| # | נושא | פירוט |
|---|---|---|
| ~~2.1~~ | ~~גודל תשובת `getExamQuestions`~~ | ✅ נסגר 22/09/2026 — הפעולה הוסרה; הטקסטים מגיעים מה-Worker (30 שאלות × 7 שפות ≈ 100KB) |
| ~~2.2~~ | ~~פער בחימום המטמון~~ | ✅ נסגר 22/09/2026 — אין מטמון ואין חימום |
| ~~2.4~~ | ~~בנק מוגן בטוקן (חלופה T)~~ | ✅ **מומש 21/09/2026 בערב** לפי החלטת יוסי (`DESIGN_2026-09-21.md` §11): הבנק לא ציבורי, מוגש מה-Worker לפי אישור חתום |
| ~~2.6~~ | ~~ערוץ push לנבחן (Durable Object + WebSocket)~~ | ✅ **מומש אחרת, 21/09 ערב:** סקר ארוך (long polling) ב-`session-gateway` — הבקשה נשארת פתוחה עד 25 שנ׳ ונענית ברגע שההחלטה נכנסת (`wait` + `fp`; DESIGN §12, STATE §7). WebSocket / Durable Objects נדחו: מגבלת המעבד בתוכנית החינמית וטלפונים שהורגים חיבור קבוע |
| ~~2.7~~ | ~~מתג סקר נפרד (`GATEWAY_POLL_OFF`)~~ | הוסר 21/09 לפי יוסי: אין מתגים חלקיים. ה-Worker חובה; הנפילה לסקר ישיר אוטומטית בלקוח; חזרה מלאה ל-r24 היא הנתיב אם ה-Worker נופל לזמן ארוך |
| 2.5 | ארכוב `תוצאות תרגול` (109k שורות) | `teacherClassDetails` קורא מאז 22/09 רק מאז יצירת הכיתה; ארכוב מלא = צעד הבא אם המורים מדווחים איטיות |
| 2.3 | ארכיון כיתות שנמחקו — רק קדימה | מחיקות היסטוריות עדיין מציגות "מורה: לא ידוע". שקול מילוי חד-פעמי אם זה מפריע בדו"חות |

---

## 3. בנוי אך לא הופעל

| # | נושא | מצב |
|---|---|---|
| 3.1 | **שכבת BI חיזויית** | מנוע כיול הסתברות מעבר בנוי ונפרס. **חסם: להריץ את תצוגת המקדימה בקונסולה ולאמת שיש אות אמיתי לפני שבונים UI נוסף.** אל תבנה ממשק על מודל שלא אומת |

---

## 4. מתוכנן, טרם התחיל

| # | נושא | מצב |
|---|---|---|
| 4.1 | **SMS לכל הנבחנים בסוף יום** | לחיצה אחת ששולחת לכל נבחן קישור לתעודה הקיימת שלו + טיימר השהיה מבוטל-לפי-בקשה. ~90% מהתשתית קיימת (שליחה מרוכזת). **חסם: ספק ה-SMS נועל לכתובת IP סטטית אחת, ול-Apps Script אין IP קבוע — נדרש ממסר. נשלחה פנייה לספק ב-22/06/2026, טרם התקבל מענה** |
| 4.3 | **עמידות כש-Cloudflare למטה** (הסקר והבנק תלויים ב-`session-gateway`; אין נפילה לסקר ישיר) | נדחה במפורש (יוסי 21/09 ערב: "Cloudflare הוא הדרך; תמיכה למקרה ש-Cloudflare לא עובד זה משהו אחר, לחשוב אחר כך"). היום: חזרה לאחור לפי DEPLOY §8. אם יוחלט לבנות — לא מסלול ישיר לגוגל (סערת 16/09), אלא עותק של הבנק במקום שני |
| 4.2 | **סשן רב-אתרי** | בוחן מוסיף אתר שני עם מכסה משלו; נבחן בוחר את האתר שלו; דו"ח מפוצל לפי אתר + כולל. סיכון נמוך (האתר כבר פר-סשן, המכסה אינה נאכפת). **לא להתחיל בלי אישור מפורש** |

---

## 5. אבטחה — נדחה במודע

הבעלים תיעדף **נכונות לפני אבטחה**. הפריטים הבאים נבחנו ונדחו לעת עתה:

| # | נושא |
|---|---|
| 5.1 | הקשחת אבטחה כללית (קבוצה 2 בסקירה) |
| 5.2 | `LockService` למניעת תנאי מרוץ בכתיבה לגיליון (קבוצה 3) |

מודל האיום בפועל: המבחן מפוקח פיזית על-ידי בוחן. הניקוד בצד השרת ואימות השאלות שנמסרו כבר מונעים את הזיוף המשמעותי. `origin` הוא הרתעה רכה ובר-עקיפה **במכוון**.

---

## 6. הוחלט לא לעשות — אל תציע מחדש

| נושא | למה |
|---|---|
| חסימת צילומי מסך בצד הנבחן | לא ניתן בדפדפן; מנגנון היציאה-מהמסך כבר תופס שליחה מאותו מכשיר; מכשיר שני הוא באחריות המשגיח |
| כפיית מספר וואטסאפ עסקי ב-iOS | אין ממשק כזה ב-iOS. אנדרואיד ומחשב פתורים |
| השהיית טיימר בנפילת רשת | וקטור רמאות — נבחן יכול לנתק רשת כדי לעצור זמן |
| צמצום אישור הבוחן לבנק (בנק מלא רק בעברית / מכסה יומית) | יוסי 21/09: בוחן מחובר רשאי להוריד את הבנק המלא (בלי תשובות) — חיפוש התמונה צריך את כל השפות, והסיכון קטן. KNOWN_ISSUES #25 |

---

## 7. ניקוי קוד מת

| # | נושא |
|---|---|
| ~~7.1~~ | ~~`student.html` stubs~~ | ✅ נמחק 22/09/2026 |
| 7.2 | `server/src/98_migration_practice.js` | למחוק אחרי 27/09 (ראה 0.3) |
| ~~7.3~~ | ~~`checkApproval` / `getExamStatus` בשרת~~ | ✅ 21/09 ערב (יוסי: לא דוחים): שתי הפעולות פרשו מה-API (→ `client_outdated`); הפונקציות נשארות כאמת-מידה לבדיקות החוזים |

---

## 8. חוב תיעוד

- [ ] לאמת את סכימת העמודות של `תוצאות תרגול` ו`התקדמות תלמידים` מול הקוד (ב-`DATA_MODEL.md` הן ברמה גבוהה בלבד)
- [ ] לתעד את מבנה ה-blueprint (`EXAM_STRUCTURE_SERVER`) לכל דרגה
- [ ] `signs_data.js` (~5MB) — לתעד מקור ואופן יצירה

ארבעת הדפים המרכזיים (בוחן, נבחן, מורה, תלמיד) מתועדים במלואם ב-[`PAGES.md`](PAGES.md).
